# 🚀 Aetheris Community

Современная платформа для сообщества с системой статей, комментариев и уведомлений.

## 🛠️ Технологии

- **Frontend**: Vue.js 3 + TypeScript + PrimeVue
- **Backend**: FastAPI + Python
- **База данных**: PostgreSQL (Supabase)
- **Деплой**: Render

## 🚀 Быстрый деплой

### Через Render Blueprint (рекомендуется)

1. **Загрузи код в GitHub:**
   ```bash
   git add .
   git commit -m "Ready for deploy"
   git push origin main
   ```

2. **Деплой на Render:**
   - Зайди на [render.com](https://render.com)
   - Нажми **"New"** → **"Blueprint"**
   - Подключи GitHub репозиторий
   - Выбери `Aetheris-community`
   - Нажми **"Apply"**

3. **Готово!** 🎉
   - Приложение: `https://aetheris-community.onrender.com`
   - API: `https://aetheris-community.onrender.com/api/articles/`

## 📋 Что включено

- ✅ Система статей с тегами
- ✅ Комментарии и ответы
- ✅ Лайки и реакции
- ✅ Система уведомлений
- ✅ JWT аутентификация
- ✅ Адаптивный дизайн
- ✅ Многоязычность

## 🔧 Локальная разработка

### Backend (одной командой из корня):
```bash
cd /Users/zimbazo/WebstormProjects/Aetheris-community
ENV=development FRONTEND_URL=http://localhost:5173 uvicorn backend.main:app --reload
```

### Frontend:
```bash
cd frontend
npm install
npm run dev
```

### Переменные окружения (DEV/PROD)

- Файл `.env.local` в корне (не коммитится) — для локальной разработки.
  - Если `DATABASE_URL` не указан, backend автоматически использует SQLite: `backend/articles.db`.
  - Можно указать локальный Postgres:
    ```
    DATABASE_URL=postgresql://user:pass@localhost:5432/aetheris_local?sslmode=disable
    ```
- Продакшн (Render) использует `DATABASE_URL` из настроек Render (Supabase URI с `sslmode=require`).

Рекомендуемые переменные:
- `JWT_SECRET` — в проде обязателен (>= 32 символов). В dev может генерироваться автоматически.
- `HOSTS` — список доверенных хостов через запятую (для TrustedHostMiddleware).
- `FORCE_HTTPS=true` — включить редирект на HTTPS в проде.

## 📖 Документация

- [BLUEPRINT_DEPLOY.md](BLUEPRINT_DEPLOY.md) - Подробная инструкция по деплою
- [NOTIFICATIONS_README.md](NOTIFICATIONS_README.md) - Система уведомлений

## 🎯 Особенности

- **Автоматический деплой** через Render Blueprint
- **Готовая база данных** на Supabase
- **Система уведомлений** о лайках
- **Современный UI** с PrimeVue
- **TypeScript** для типобезопасности