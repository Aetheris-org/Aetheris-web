# ✅ Исправления проблем с CreateArticle

## 🔧 Исправленные проблемы

### 1. Страница не загружается без перезагрузки
**Проблема**: Компонент CreateArticle не монтировался при первом переходе на `/create-article`

**Решение**:
- ✅ Добавлена проверка авторизации в `onMounted` компонента
- ✅ Добавлена поддержка редиректа после авторизации (`sessionStorage`)
- ✅ Исправлен router guard для корректной обработки защищённых маршрутов

**Файлы**:
- `frontend/src/views/CreateArticle.vue` - добавлена проверка авторизации
- `frontend/src/router/index.ts` - улучшена обработка redirect
- `frontend/src/views/Auth.vue` - сохранение redirect в sessionStorage

---

### 2. Статья не появляется на главной после создания
**Проблема**: После создания статьи она не отображалась на главной странице, хотя была в Strapi

**Решение**:
- ✅ Добавлено событие `articles:refresh` для обновления списка статей
- ✅ Добавлен watch на `route.query.refresh` для принудительного обновления
- ✅ Добавлен watch на `route.path` для обновления при переходе на главную
- ✅ Исправлена функция `transformArticle` для правильного преобразования данных из Strapi
- ✅ Исправлена функция `mapServerArticle` для работы с новым форматом `author` (объект вместо строки)

**Файлы**:
- `frontend/src/views/CreateArticle.vue` - отправка события `articles:refresh` и переход с query параметром
- `frontend/src/views/HomePage.vue` - обработчик события и watch на route
- `frontend/src/api/articles.ts` - исправлен `transformArticle` для возврата объекта `author`
- `frontend/src/composables/useArticles.ts` - упрощён `mapServerArticle` для работы с новым форматом

---

## 🧪 Как проверить

### Проверка 1: Загрузка страницы создания статьи
1. Выйди из аккаунта (если авторизован)
2. Перейди на `http://localhost:5173/create-article`
3. **Ожидание**: Должен произойти редирект на `/auth?redirect=/create-article`
4. Авторизуйся через Google
5. **Ожидание**: После авторизации должен произойти автоматический редирект на `/create-article`
6. **Ожидание**: Страница должна загрузиться БЕЗ перезагрузки браузера

### Проверка 2: Создание статьи
1. Авторизуйся и перейди на `/create-article`
2. Заполни форму:
   ```
   Заголовок: "Тестовая статья"
   Содержание: "Это тестовое содержание"
   Теги: ["JavaScript", "Vue.js"]
   Сложность: Medium
   ```
3. Нажми "Опубликовать"
4. **Ожидание**: Должен произойти переход на главную страницу
5. **Ожидание**: Новая статья должна появиться в списке статей БЕЗ перезагрузки страницы

---

## 🔍 Отладка

### Проверка консоли браузера
Открой DevTools (F12) → Console и проверь логи:

```
🔵 CreateArticle mounted
✅ User authenticated: { id: 4, username: "test_user" }
```

При создании статьи:
```
ArticleData перед API: {...}
✅ Article created successfully: {...}
🔄 Refreshing articles list...
```

При переходе на главную:
```
🔄 Route refresh query detected, refreshing articles...
🔄 Refreshing articles list...
Статьи загружены следующие: [...]
```

### Проверка API напрямую
```bash
# Получить список статей
curl "http://localhost:1337/api/articles?filters[publishedAt][\$notNull]=true&populate[author][fields][0]=id&populate[author][fields][1]=username&populate[author][populate][avatar][fields][0]=url"

# Должен вернуть список статей, включая только что созданную
```

---

## 📋 Чек-лист исправлений

- ✅ Проверка авторизации в CreateArticle.vue
- ✅ Поддержка redirect после авторизации
- ✅ Событие `articles:refresh` для обновления списка
- ✅ Watch на route.query.refresh для принудительного обновления
- ✅ Watch на route.path для обновления при переходе на главную
- ✅ Исправлен transformArticle для возврата объекта author
- ✅ Исправлен mapServerArticle для работы с новым форматом
- ✅ Добавлена задержка 500ms перед обновлением списка (для индексации в Strapi)
- ✅ Добавлены все необходимые поля для совместимости с типом Article

---

## 🎯 Следующие шаги

1. **Протестируй создание статьи** - убедись, что всё работает без перезагрузки
2. **Проверь отображение статьи** - убедись, что автор, теги, превью отображаются правильно
3. **Проверь редактирование** - создай статью и попробуй отредактировать
4. **Проверь удаление** - создай статью и попробуй удалить с отменой

**Всё должно работать без перезагрузки страницы!** 🚀

