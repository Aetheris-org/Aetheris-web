# 🚀 Локальный запуск проекта

## Текущий статус

- ✅ **Frontend**: Запущен на http://localhost:5173
- ❌ **Backend**: Требуется установка зависимостей (проблема с сетью при установке Prisma engines)

## Быстрый запуск

### 1. Frontend (уже запущен)

```bash
source ~/.nvm/nvm.sh
cd frontend-react
npm run dev
```

**Доступен на:** http://localhost:5173

### 2. Backend (требует установки зависимостей)

**Проблема:** При установке зависимостей возникает ошибка `ECONNRESET` при загрузке `@prisma/engines`.

**Решение:**

1. **Попробуйте установить зависимости вручную:**
```bash
source ~/.nvm/nvm.sh
cd backend/keystonejs-backend
npm install
```

2. **Если проблема с сетью продолжается, попробуйте:**
```bash
# Очистить кэш npm
npm cache clean --force

# Установить с увеличенными таймаутами
npm install --fetch-retries=10 --fetch-retry-factor=2 --fetch-retry-mintimeout=30000

# Или использовать другой registry
npm config set registry https://registry.npmjs.org/
npm install
```

3. **После успешной установки запустите backend:**
```bash
cd backend/keystonejs-backend
npm run dev
```

**Backend будет доступен на:**
- API: http://localhost:1337/api/graphql
- Admin UI: http://localhost:1337/

## Настройка переменных окружения

### Backend (`backend/keystonejs-backend/.env`)

Минимальная конфигурация для development:

```env
# Database (SQLite для development)
DATABASE_URL="file:./.tmp/data.db"

# Security (минимум 32 символа)
SESSION_SECRET="your-very-long-secret-key-minimum-32-characters-long"
EMAIL_HMAC_SECRET="your-very-long-secret-key-minimum-32-characters-long"

# OAuth2 Google (опционально для локальной разработки)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:1337/api/connect/google/callback"

# URLs
FRONTEND_URL="http://localhost:5173"
PUBLIC_URL="http://localhost:1337"

# Environment
NODE_ENV="development"
LOG_LEVEL="debug"
PORT="1337"
```

### Frontend (`frontend-react/.env`)

```env
VITE_API_BASE_URL=http://localhost:1337
VITE_FRONTEND_URL=http://localhost:5173
```

## Проверка работы

1. **Frontend:** Откройте http://localhost:5173
2. **Backend API:** Проверьте http://localhost:1337/api/graphql
3. **Admin UI:** Откройте http://localhost:1337/

## Устранение проблем

### Проблема: ECONNRESET при установке зависимостей

**Причины:**
- Нестабильное интернет-соединение
- Проблемы с npm registry
- Таймауты при загрузке больших пакетов (Prisma engines)

**Решения:**
1. Проверьте интернет-соединение
2. Попробуйте позже (может быть временная проблема с npm)
3. Используйте VPN, если есть ограничения
4. Попробуйте установить зависимости по частям

### Проблема: Backend не запускается

1. Проверьте, что все зависимости установлены: `ls node_modules`
2. Проверьте `.env` файл
3. Проверьте логи: `npm run dev` покажет ошибки

## Альтернативный способ запуска

Если проблемы с npm продолжаются, можно использовать Docker (если настроен) или установить зависимости на другой машине и скопировать `node_modules`.

