# ✅ Свежий старт - База данных пересоздана

## Что произошло

База данных Strapi была **полностью пересоздана** из-за критических ошибок:
1. ❌ `"Undefined attribute level operator id"` - конфликт в схеме базы данных
2. ❌ `"no such column: t0.email"` - отсутствовали базовые поля User
3. ✅ **ИСПРАВЛЕНО:** Создана правильная схема User с базовыми полями Strapi + кастомными полями (bio, avatar, relations)

Теперь всё чисто и готово к использованию!

---

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Создай admin аккаунт

1. Открой http://localhost:1337/admin
2. **Создай первого администратора:**
   - Имя: `Admin`
   - Email: `admin@example.com`
   - Пароль: `Admin123!` (или любой другой безопасный пароль)
3. Нажми **"Let's start"**

### Шаг 2: Настрой разрешения (ВАЖНО!)

После входа в админку:

1. **Левое меню** → Settings (⚙️) → Users & Permissions → Roles

2. **Роль "Public"** (для незарегистрированных пользователей):
   - **Article**: включи `find`, `findOne`, `search`
   - **Comment**: включи `find`
   - **Upload**: включи `upload`
   - Нажми **Save**

3. **Роль "Authenticated"** (для залогиненных пользователей):
   - **Article**: ✅ ВСЕ чекбоксы (`find`, `findOne`, `create`, `update`, `delete`, `react`, `search`, `getUserReaction`)
   - **Comment**: ✅ ВСЕ чекбоксы (`find`, `findOne`, `create`, `update`, `delete`, `react`)
   - **Article-bookmark**: ✅ ВСЕ чекбоксы (`find`, `findOne`, `create`, `delete`, `toggle`, `check`, `getBookmarkedArticles`)
   - **Notification**: ✅ ВСЕ чекбоксы (`find`, `findOne`, `markAsRead`, `getUnreadCount`)
   - **Upload**: ✅ включи `upload`
   - Нажми **Save**

### Шаг 3: Протестируй OAuth

1. Открой http://localhost:5173
2. Нажми **"Войти через Google"**
3. Авторизуйся
4. Установи nickname
5. Готово! ✅

---

## 📊 Статус

| Компонент | Статус | URL |
|-----------|--------|-----|
| **Strapi Backend** | ✅ Работает | http://localhost:1337 |
| **Strapi Admin** | ✅ Работает | http://localhost:1337/admin |
| **Frontend** | ⏳ Требует запуска | http://localhost:5173 |

---

## 🔧 Запуск сервисов

### Backend (Strapi)
```bash
cd backend/strapi-backend
npm run develop
```

### Frontend (Vue.js)
```bash
cd /Users/zimbazo/WebstormProjects/Aetheris-community
npm run dev
```

**Важно:** Backend должен быть запущен ПЕРЕД frontend!

---

## 🎯 Что работает

После настройки разрешений будет доступно:

### Для всех (Public):
- ✅ Просмотр списка статей
- ✅ Просмотр отдельной статьи
- ✅ Поиск статей
- ✅ Просмотр комментариев

### Для авторизованных (Authenticated):
- ✅ Создание статей
- ✅ Редактирование своих статей
- ✅ Удаление своих статей
- ✅ Лайк/дизлайк статей и комментариев
- ✅ Комментирование
- ✅ Закладки
- ✅ Уведомления
- ✅ Загрузка аватаров и изображений

---

## 📝 Важные endpoints

### OAuth
- `GET /api/connect/google` - Вход через Google
- `GET /api/connect/google/callback` - OAuth callback
- `PUT /api/users/me` - Обновить профиль

### Статьи
- `GET /api/articles` - Список статей
- `GET /api/articles/:id` - Одна статья
- `POST /api/articles` - Создать статью (auth required)
- `POST /api/articles/:id/react` - Лайк/дизлайк
- `GET /api/articles/search?q=query` - Поиск

### Комментарии
- `GET /api/comments?filters[article][id][$eq]=:id` - Комментарии к статье
- `POST /api/comments` - Создать комментарий
- `POST /api/comments/:id/react` - Лайк/дизлайк

### Закладки
- `POST /api/articles/:id/bookmark` - Добавить/убрать
- `GET /api/articles/bookmarked` - Список закладок

### Уведомления
- `GET /api/notifications` - Список
- `POST /api/notifications/:id/read` - Отметить прочитанным
- `GET /api/notifications/unread-count` - Количество непрочитанных

---

## 🔒 Безопасность

Все меры безопасности активны:
- ✅ Google OAuth2
- ✅ Email hashing (HMAC-SHA256)
- ✅ JWT (15 мин) + Refresh Tokens (7 дней)
- ✅ CSRF Protection
- ✅ Rate Limiting (500/мин + 10/мин OAuth)
- ✅ Security Headers (CSP, HSTS, X-Frame-Options)
- ✅ HttpOnly Cookies

---

## 🐛 Troubleshooting

### Проблема: "An error occurred" в админке
**Решение:** Убедись, что ты создал admin аккаунт через http://localhost:1337/admin

### Проблема: 403 Forbidden при создании статьи
**Решение:** Проверь разрешения для роли "Authenticated" (см. Шаг 2)

### Проблема: OAuth не работает
**Решение:** 
1. Проверь `.env` файл - должны быть `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET`
2. Убедись, что в Google Console добавлен redirect URI: `http://localhost:1337/api/connect/google/callback`

### Проблема: Меня перебрасывает на страницу авторизации
**Решение:**
1. Очисти cookies: `document.cookie = 'accessToken=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT'`
2. Очисти localStorage: `localStorage.clear()`
3. Перезагрузи страницу
4. Войди заново через Google

---

## ✅ Чеклист первого запуска

- [ ] Strapi запущен (http://localhost:1337)
- [ ] Admin аккаунт создан
- [ ] Разрешения настроены (Public + Authenticated)
- [ ] Frontend запущен (http://localhost:5173)
- [ ] OAuth протестирован
- [ ] Статья создана
- [ ] Комментарий добавлен
- [ ] Лайк поставлен
- [ ] Всё работает! 🎉

---

**Следующий шаг:** Создай admin аккаунт на http://localhost:1337/admin

