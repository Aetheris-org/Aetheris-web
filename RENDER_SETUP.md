# 🔧 НАСТРОЙКА RENDER ДЛЯ ДЕПЛОЯ

## 🚨 ВАЖНО: После деплоя Blueprint нужно настроить переменные!

### Шаг 1: Деплой Blueprint
1. Зайди на [render.com](https://render.com)
2. Нажми **"New"** → **"Blueprint"**
3. Подключи GitHub репозиторий `Aetheris-community`
4. Нажми **"Apply"**

### Шаг 2: Настройка переменных окружения
После деплоя Blueprint:

1. **Зайди в Dashboard Render**
2. **Найди свой сервис `aetheris-community`**
3. **Перейди в "Environment"**
4. **Добавь переменную `DATABASE_URL`:**

```
DATABASE_URL=postgresql://postgres:uYRqNyva7vhczzEW@db.tlrbcosqunqdbuvovgog.supabase.co:5432/postgres
```

5. **Нажми "Save Changes"**
6. **Перезапусти сервис** (кнопка "Manual Deploy")

### Шаг 3: Проверка работы
После настройки переменных:
- Приложение: `https://aetheris-community.onrender.com`
- API: `https://aetheris-community.onrender.com/api/articles/`

---

## 🔍 РЕШЕНИЕ ПРОБЛЕМ

### Если деплой не работает:

1. **Проверь логи** в Render Dashboard → твой сервис → "Logs"
2. **Убедись, что `DATABASE_URL` установлена** в Environment Variables
3. **Проверь, что Supabase база активна** (зайди в Supabase Dashboard)

### Если база данных недоступна:

1. **Зайди в Supabase Dashboard**
2. **Перейди в Settings → Database**
3. **Скопируй новый Connection String**
4. **Обнови `DATABASE_URL` в Render**

---

## ✅ ЧТО ИСПРАВЛЕНО

- ✅ Убрано принудительное создание таблиц при старте
- ✅ Добавлена обработка ошибок подключения к БД
- ✅ Настроен `sync: false` для `DATABASE_URL`
- ✅ Миграции Alembic будут создавать таблицы

Теперь приложение запустится даже если база данных временно недоступна!
