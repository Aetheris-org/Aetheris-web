# 🔧 Настройка Permissions в Strapi Admin Panel

## ❌ Проблема

После удаления автоматической настройки permissions, нужно настроить их вручную. Сейчас возникают ошибки 403:
- `GET /api/users/me` → 403 Forbidden
- `POST /api/articles` → 403 Forbidden

## ✅ Решение: Настроить Permissions вручную

### Шаг 1: Открой Strapi Admin Panel

1. Зайди в `http://localhost:1337/admin`
2. Авторизуйся (если нужно)

### Шаг 2: Настрой Public роль (для неавторизованных пользователей)

1. Settings → Users & Permissions → Roles → **Public**
2. Прокрути вниз до **Article**
3. Включи галочки:
   - ✅ **find** (чтение списка статей)
   - ✅ **findOne** (чтение одной статьи)
4. Прокрути до **Comment**
5. Включи галочки:
   - ✅ **find** (чтение списка комментариев)
   - ✅ **findOne** (чтение одного комментария)
6. Нажми **Save** вверху страницы

### Шаг 3: Настрой Authenticated роль (для авторизованных пользователей)

1. Settings → Users & Permissions → Roles → **Authenticated**
2. Прокрути вниз до **Article**
3. Включи галочки:
   - ✅ **find** (чтение списка статей)
   - ✅ **findOne** (чтение одной статьи)
   - ✅ **create** (создание статей)
   - ✅ **update** (редактирование статей)
   - ✅ **delete** (удаление статей)
4. Прокрути до **Comment**
5. Включи галочки:
   - ✅ **find** (чтение списка комментариев)
   - ✅ **findOne** (чтение одного комментария)
   - ✅ **create** (создание комментариев)
   - ✅ **update** (редактирование комментариев)
   - ✅ **delete** (удаление комментариев)
6. Прокрути до **Article-Bookmark**
7. Включи галочки:
   - ✅ **find** (чтение закладок)
   - ✅ **findOne** (чтение одной закладки)
   - ✅ **create** (создание закладок)
   - ✅ **update** (редактирование закладок)
   - ✅ **delete** (удаление закладок)
8. Прокрути до **Notification**
9. Включи галочки:
   - ✅ **find** (чтение уведомлений)
   - ✅ **findOne** (чтение одного уведомления)
   - ✅ **create** (создание уведомлений)
   - ✅ **update** (редактирование уведомлений)
   - ✅ **delete** (удаление уведомлений)
10. **ВАЖНО**: Прокрути вверх до **Users-Permissions** (это для `/api/users/me`)
11. Включи галочку:
    - ✅ **me** (получение информации о текущем пользователе)
12. Нажми **Save** вверху страницы

### Шаг 4: Проверь результат

1. Разлогинься (если авторизован)
2. Открой `http://localhost:5173/`
3. **Ожидаемый результат**: Статьи отображаются (Public роль работает)

4. Авторизуйся через Google OAuth
5. Открой `http://localhost:5173/create-article`
6. Заполни форму и нажми "Опубликовать"
7. **Ожидаемый результат**: Статья создаётся успешно (Authenticated роль работает)

## 🔍 Важно про поля (fields)

В Strapi v5, когда настраиваешь permissions через Admin Panel:
- Если **НЕ указываешь поля** (fields) → доступ ко **ВСЕМ** полям ✅
- Если **указываешь пустой список** полей → доступ к **НУЛЮ** полей ❌

**Рекомендация**: Когда включаешь галочки для действий (find, create, update), **НЕ настраивай поля отдельно** - оставь всё как есть по умолчанию.

## 📝 Альтернативный способ (через API)

Если хочешь настроить через API, можно использовать Strapi REST API или GraphQL, но проще через Admin Panel.

## ✅ Готово!

После настройки permissions через Admin Panel всё должно заработать!

