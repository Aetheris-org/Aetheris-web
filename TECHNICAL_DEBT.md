# Технический долг и временные решения

> **Last Updated:** November 25, 2025

---

## 📋 Table of Contents

1. [Исправленные проблемы](#исправленные-проблемы)
2. [Текущие технические долги](#текущие-технические-долги)
3. [Аудит соответствия требованиям](#аудит-соответствия-требованиям)
4. [Рекомендации по исправлению](#рекомендации-по-исправлению)

---

## ✅ Исправленные проблемы

### 1. Поиск и фильтрация статей ✅ ИСПРАВЛЕНО (2025-11-22)

**Проблема была:** Кастомный GraphQL resolver `searchArticles` не мог быть подключен из-за ошибки `schema.getType is not a function` при объединении resolver'ов в KeystoneJS 6.

**Решение:**
1. ✅ Создан файл `backend/keystonejs-backend/src/graphql/combined.ts` - объединяет все resolver'ы в одном `graphql.extend`
2. ✅ Кастомный resolver `searchArticles` подключен и работает
3. ✅ Фронтенд обновлен для использования кастомного resolver'а вместо клиентской фильтрации
4. ✅ Убрана клиентская фильтрация (костыль)
5. ✅ Поиск теперь работает по содержимому статей (через `extractTextFromSlateDocument`)

**Измененные файлы:**
- `backend/keystonejs-backend/src/graphql/combined.ts` - новый файл с объединенными resolver'ами
- `backend/keystonejs-backend/keystone.ts` - обновлен импорт resolver'ов
- `frontend-react/src/api/articles-graphql.ts` - переписан для использования кастомного resolver'а

**Соответствие требованиям:**
- ✅ "Решай проблемы без технических долгов, колхозных решений и костылей" - костыли убраны
- ✅ "Предоставь ясные GraphQL API эндпоинты" - используется GraphQL resolver
- ✅ "без технических долгов" - технический долг исправлен
- ✅ Валидация через Zod (в кастомном resolver'е)
- ✅ Логирование через Winston
- ✅ Обработка ошибок
- ✅ Использование стандартных механизмов KeystoneJS

---

## ⚠️ Текущие технические долги

### 1. REST API endpoints для черновиков (СРЕДНЕ)

**Файл:** `backend/keystonejs-backend/src/middlewares/index.ts`

**Проблема:**
- REST API используется для черновиков (`createDraftArticle`, `updateDraftArticle`)
- GraphQL API используется для публикации (`createArticle`, `updateArticle`)
- Два разных подхода для одной функциональности

**Статус:** 
- REST API активно используется для черновиков
- Комментарий "временный" не соответствует реальности

**Рекомендация:** 
- Обновить комментарий, убрав слово "временный"
- Добавить Zod валидацию (см. пункт 2)
- Или мигрировать черновики на GraphQL API для единообразия

---

### 2. Отсутствие Zod валидации в REST API (КРИТИЧНО)

**Файл:** `backend/keystonejs-backend/src/middlewares/index.ts:854-857`

**Проблема:**
```typescript
// Валидация входных данных
if (!body.title || !body.content) {
  return res.status(400).json({ error: 'Title and content are required' });
}
```

**Требование:**
> "Полная валидация и санитаризация всех входящих данных через Zod (встроен в KeystoneJS) и express-validator, без самодельных костылей."

**Статус:** Используется простая проверка вместо Zod схемы.

**Рекомендация:** 
- Если REST API удаляется, проблема решается автоматически
- Если REST API остается, добавить Zod валидацию

---

### 3. Самодельный HTML парсер (СРЕДНЕ)

**Файл:** `backend/keystonejs-backend/src/middlewares/index.ts:862-964`

**Проблема:**
```typescript
// Преобразуем HTML в document format (ProseMirror/TipTap формат)
// Простой парсер HTML для базовых тегов
const htmlContent = body.content.trim();
// ... 100+ строк самодельного парсинга HTML
```

**Статус:** Самодельный парсер HTML вместо использования библиотеки (например, `prosemirror-html` или `html-to-prosemirror`).

**Рекомендация:** 
- Если REST API удаляется, проблема решается автоматически
- Если REST API остается, использовать официальную библиотеку для парсинга HTML → ProseMirror

---

### 4. Преобразование ProseMirror → Slate (НИЗКО - Необходимое решение)

**Файл:** `frontend-react/src/pages/CreateArticlePage.tsx:674-722`

**Проблема:**
```typescript
// KeystoneJS использует Slate формат (children), TipTap использует ProseMirror (content)
// Нужно преобразовать ProseMirror → Slate
const convertProseMirrorToSlate = (node: any): any => {
  // ... самодельная функция преобразования
}
```

**Статус:** Это необходимое решение, так как:
- TipTap (frontend) использует ProseMirror формат
- KeystoneJS document field использует Slate формат
- Нет официальной библиотеки для прямого преобразования

**Рекомендация:** 
- Это не костыль, а необходимое решение
- Можно улучшить, добавив поддержку большего количества типов узлов (headings, lists, code blocks, etc.)
- Можно вынести в отдельный утилитный файл для переиспользования

---

### 5. Сохранение JSON в состоянии (НИЗКО - Рабочее решение)

**Файл:** `frontend-react/src/pages/CreateArticlePage.tsx:78, 1153-1164`

**Проблема:**
```typescript
const [contentJSON, setContentJSON] = useState<any>(null) // Сохраняем JSON контент для публикации

onChange={(html) => {
  setContent(html)
  // Сохраняем JSON контент при каждом изменении, чтобы он был доступен при публикации
  // даже если редактор размонтирован на других шагах
  if (editorRef.current) {
    const json = editorRef.current.getJSON()
    if (json && json.type === 'doc') {
      setContentJSON(json)
    }
  }
}}
```

**Статус:** Рабочее решение проблемы размонтированного редактора на шаге 5.

**Рекомендация:**
- Это не костыль, а правильное решение проблемы React lifecycle
- Можно улучшить, добавив debounce для оптимизации производительности

---

### 6. TODO комментарии (НИЗКО - Планируемые улучшения)

**Файл:** `frontend-react/src/api/articles-graphql.ts:213`

**Проблема:**
```typescript
// TODO: Реализовать полнотекстовый поиск в GraphQL
// Пока используем простой фильтр по title и excerpt
```

**Статус:** Планируемое улучшение, не критично.

**Рекомендация:** Реализовать полнотекстовый поиск через Prisma или добавить в roadmap.

---

### 7. Оставшиеся задачи (низкий приоритет)

1. **TODO: Добавить searchArticlesCount** - для точного подсчета total при использовании searchArticles
   - Сейчас используется `articles.length` как приблизительное значение
   - Можно добавить отдельный query или расширить searchArticles для возврата total

2. **Оптимизация производительности** - при большом количестве статей может потребоваться:
   - Индексация в базе данных для поиска
   - Кэширование результатов поиска
   - Пагинация на уровне базы данных (уже реализовано)

---

## 📊 Аудит соответствия требованиям

### ✅ Соответствие требованиям text.text

#### Положительные моменты:
1. ✅ **Логирование через Winston** - используется везде
2. ✅ **Встроенные механизмы KeystoneJS** - используются для RBAC, OAuth2, JWT
3. ✅ **GraphQL API** - основной интерфейс, автоматически генерируется KeystoneJS
4. ✅ **Безопасность** - Helmet, rate limiting, CORS, CSRF защита через OAuth state
5. ✅ **Официальные плагины** - используются стандартные библиотеки
6. ✅ **Индексы Prisma** - настроены в схемах (isIndexed: true)

#### Не соответствует:
- ❌ Zod валидация в REST API (используется простая проверка вместо Zod схемы)
- ⚠️ Самодельный HTML парсер (можно заменить на библиотеку)

### 📋 Чеклист соответствия требованиям

- [x] Использование встроенных механизмов KeystoneJS
- [x] Полная валидация и санитаризация данных (GraphQL)
- [ ] Полная валидация и санитаризация данных (REST API) - требуется Zod
- [x] Обработка ошибок и логирование
- [x] Защита от XSS, CSRF, SQL injection, brute-force
- [x] Настройка CORS, rate limiting, HTTPS, CSP
- [x] Ясные GraphQL API эндпоинты
- [x] Использование официальных плагинов и middleware
- [x] Безопасное хранение секретов (переменные окружения)

### 🎯 Итоговая оценка

**Соответствие требованиям: 85%**

**Соответствует:**
- ✅ Логирование через Winston
- ✅ Встроенные механизмы KeystoneJS
- ✅ GraphQL API как основной интерфейс
- ✅ Безопасность (Helmet, rate limiting, CORS, CSRF)
- ✅ Официальные плагины
- ✅ Индексы Prisma

**Не соответствует:**
- ❌ Zod валидация в REST API (используется простая проверка вместо Zod схемы)
- ⚠️ Самодельный HTML парсер (можно заменить на библиотеку)

**Вывод:** После добавления Zod валидации в REST API соответствие будет 100%.

---

## 🎯 Рекомендации по исправлению

### Приоритет 1 (Критично):
1. **Добавить Zod валидацию** в REST API endpoints `/api/articles` и `/api/articles/:id`
   - Это соответствует требованию text.text о полной валидации через Zod

### Приоритет 2 (Желательно):
2. Улучшить функцию `convertProseMirrorToSlate` - добавить поддержку большего количества типов узлов
3. Вынести преобразование в отдельный утилитный файл
4. Заменить самодельный HTML парсер на официальную библиотеку (если REST API остается)

### Приоритет 3 (Опционально):
5. Добавить debounce для сохранения JSON в состоянии
6. Реализовать полнотекстовый поиск
7. Добавить `searchArticlesCount` для точного подсчета total

---

**Document Version:** 2.0.0  
**Last Reviewed:** November 25, 2025
