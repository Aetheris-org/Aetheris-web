# 🚀 Strapi Migration - Quick Start Guide

## ✅ Migration Status: COMPLETE

Все компоненты успешно мигрированы с Python/FastAPI на Strapi v5.

---

## 📋 Быстрый запуск

### 1️⃣ Запуск Strapi Backend

```bash
cd backend/strapi-backend
npm run develop
```

Strapi запустится на `http://localhost:1337`

При первом запуске:
1. Создайте admin аккаунт через `http://localhost:1337/admin`
2. Настройте разрешения (см. ниже)

### 2️⃣ Запуск Frontend

```bash
# В корне проекта
npm run dev
```

Frontend запустится на `http://localhost:5173`

---

## ⚙️ Настройка разрешений (ВАЖНО!)

Перейдите в `http://localhost:1337/admin` → Settings → Users & Permissions → Roles

### Public (неавторизованные пользователи)
- **Article**: `find`, `findOne`
- **Comment**: `find`
- **Upload**: `upload` (для загрузки файлов)

### Authenticated (авторизованные пользователи)
- **Article**: ВСЕ действия (`find`, `findOne`, `create`, `update`, `delete`, `react`, `search`)
- **Comment**: ВСЕ действия (`find`, `findOne`, `create`, `update`, `delete`, `react`)
- **Article-bookmark**: ВСЕ действия
- **Notification**: ВСЕ действия
- **Upload**: `upload`

**Важно**: После настройки нажмите "Save" для каждой роли!

---

## 🔄 Миграция данных (опционально)

Если у вас есть старые данные в `backend/articles.db`:

```bash
cd backend/strapi-backend
node scripts/migrate-from-python.js
```

Скрипт мигрирует:
- ✅ Статьи (с тегами, сложностью, лайками)
- ✅ Комментарии (с вложенностью)
- ✅ Закладки
- ✅ Уведомления

---

## 🔑 OAuth Google (уже настроен)

OAuth2 через Google уже работает! Просто убедитесь, что в `.env` установлены:

```env
GOOGLE_CLIENT_ID=ваш_client_id
GOOGLE_CLIENT_SECRET=ваш_secret
GOOGLE_CALLBACK_URL=http://localhost:1337/api/connect/google/callback
EMAIL_HASH_SECRET=ваш_64_символьный_ключ
```

---

## 📁 Структура проекта после миграции

```
backend/
├── articles.db              # Старая база (можно удалить после миграции)
└── strapi-backend/          # ✅ НОВЫЙ BACKEND (Strapi)
    ├── src/
    │   ├── api/
    │   │   ├── article/     # Content-type: Статьи
    │   │   ├── comment/     # Content-type: Комментарии
    │   │   ├── notification/ # Content-type: Уведомления
    │   │   └── article-bookmark/ # Content-type: Закладки
    │   ├── extensions/
    │   │   └── users-permissions/ # OAuth + User расширения
    │   ├── policies/         # Кастомные политики
    │   ├── services/         # CSRF, Refresh Tokens, Sessions
    │   └── index.ts          # Security headers, Rate limiting
    └── scripts/
        └── migrate-from-python.js # Скрипт миграции

frontend/
├── src/
│   ├── adapters/
│   │   └── strapi.ts        # ✅ НОВЫЙ: Адаптеры для Strapi API
│   └── api/
│       ├── articles.ts      # ✅ ОБНОВЛЕНО: Работа с Strapi
│       ├── profile.ts       # ✅ ОБНОВЛЕНО: Работа с Strapi
│       └── notifications.ts # ✅ ОБНОВЛЕНО: Работа с Strapi
```

---

## 🆕 Новые API endpoints

### Статьи
- `GET /api/articles` - Список статей
- `GET /api/articles/:id` - Одна статья
- `POST /api/articles` - Создать статью
- `PUT /api/articles/:id` - Обновить статью
- `DELETE /api/articles/:id` - Удалить статью
- `POST /api/articles/:id/react` - Лайк/дизлайк
- `GET /api/articles/search?q=query` - Поиск

### Комментарии
- `GET /api/comments?filters[article][id][$eq]=:id` - Комментарии к статье
- `POST /api/comments` - Создать комментарий
- `POST /api/comments/:id/react` - Лайк/дизлайк комментария

### Закладки
- `POST /api/articles/:id/bookmark` - Добавить/убрать закладку
- `GET /api/articles/bookmarked` - Список закладок

### Уведомления
- `GET /api/notifications` - Список уведомлений
- `POST /api/notifications/:id/read` - Отметить прочитанным
- `GET /api/notifications/unread-count` - Количество непрочитанных

### Загрузка файлов
- `POST /api/upload` - Загрузить изображение (аватар, превью статьи)

---

## ✨ Что изменилось

| Было (Python) | Стало (Strapi) |
|---------------|----------------|
| imgBB для загрузки | Strapi Media Library |
| Ручные CRUD операции | Автоматические + кастомные |
| Нет админ-панели | ✅ Встроенная админка |
| Python | TypeScript |
| SQLite | SQLite (dev) / PostgreSQL (prod) |
| Ручной роутинг | Strapi REST API |

---

## 🔒 Безопасность (сохранена)

Все меры безопасности из `SECURITY.md` работают:
- ✅ Google OAuth2 с CSRF защитой
- ✅ Email hashing (HMAC-SHA256)
- ✅ JWT (15 мин) + Refresh Tokens (7 дней)
- ✅ Rate Limiting (500/мин общий, 10/мин OAuth)
- ✅ CSRF Protection на всех мутациях
- ✅ Security Headers (CSP, HSTS, X-Frame-Options)
- ✅ HttpOnly Cookies

---

## 🧪 Тестирование

### Базовые тесты
1. **OAuth вход**: Зайти через Google → должен создать пользователя
2. **Создать статью**: Написать статью, загрузить превью
3. **Лайкнуть статью**: Должен увеличиться счетчик
4. **Комментарий**: Добавить комментарий к статье
5. **Закладка**: Добавить в избранное
6. **Уведомления**: Должны создаться при комментариях/лайках

### Проверка безопасности
```bash
# Попробуйте удалить чужую статью (должно быть 403)
# Попробуйте создать 11 OAuth запросов подряд (должен сработать rate limit)
# Попробуйте POST без CSRF токена (должен быть 403)
```

---

## 🐛 Troubleshooting

### Проблема: Strapi не запускается (port 1337 занят)
```bash
# Найти процесс на порту 1337
lsof -ti:1337

# Убить процесс
lsof -ti:1337 | xargs kill -9
```

### Проблема: 403 Forbidden при создании статьи
- Проверьте разрешения для роли "Authenticated" в админке Strapi
- Убедитесь, что вы авторизованы (есть токен в cookies)

### Проблема: Превью изображений не загружаются
- Проверьте `config/middlewares.ts` - должен быть `formLimit: '5mb'`
- Проверьте права на папку `public/uploads/`

### Проблема: Старые статьи не отображаются
- Запустите миграцию: `node scripts/migrate-from-python.js`
- Проверьте, что пользователи мигрированы (должны совпадать usernames)

---

## 📚 Дополнительные ресурсы

- [MIGRATION_COMPLETE.md](./MIGRATION_COMPLETE.md) - Подробная документация миграции
- [SECURITY.md](./SECURITY.md) - Полная документация безопасности
- [Strapi Docs](https://docs.strapi.io/) - Официальная документация Strapi

---

## 🎉 Готово!

Миграция завершена успешно. Теперь у вас:
- ✅ Современный Strapi v5 backend
- ✅ TypeScript вместо Python
- ✅ Встроенная админ-панель
- ✅ Все функции сохранены
- ✅ Безопасность на том же уровне (или выше)

**Приятной работы с новым backend!** 🚀

