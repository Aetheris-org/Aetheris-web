# ✅ Исправление проблемы с permissions для авторизованных пользователей

## 🔍 Проблема

**Странное поведение**:
- ❌ Когда пользователь **НЕ авторизован** → статьи отображаются **правильно** (title, content, tags, author)
- ✅ Когда пользователь **авторизован** → статьи становятся **пустыми** (нет полей, помечены как draft)

## 🔎 Причина

В Strapi есть две роли:
1. **Public** (для неавторизованных пользователей)
2. **Authenticated** (для авторизованных пользователей)

**Проблема была в permissions для роли `Authenticated`**:

### Что происходило:

1. **Public роль** → permissions созданы правильно → поля доступны
2. **Authenticated роль** → permissions созданы **БЕЗ указания полей** → Strapi **блокирует доступ к полям**

### Почему так получилось:

В bootstrap функции permissions создавались так:

```typescript
// ❌ Неправильно: не указаны поля (fields)
await strapi.db.query('plugin::users-permissions.permission').create({
  data: {
    action: 'api::article.article.find',
    role: authenticatedRole.id,
    // fields отсутствует! Strapi блокирует доступ
  },
});
```

**Важно**: В Strapi, если поле `fields` не указано или пустое в permission, **доступ к полям блокируется** по умолчанию!

## ✅ Решение

Обновил bootstrap функцию в `backend/strapi-backend/src/index.ts`:

### Что изменилось:

1. **Удаляем ВСЕ старые permissions** для роли `authenticated`
2. **Создаём новые permissions** с правильной структурой (без явного указания `fields`, что даёт доступ ко всем полям)

```typescript
// 🔧 Удаляем старые permissions
console.log('🗑️  Removing old permissions for authenticated role...');
const oldPermissions = await strapi.db.query('plugin::users-permissions.permission').findMany({
  where: { role: authenticatedRole.id },
});

for (const oldPerm of oldPermissions) {
  await strapi.db.query('plugin::users-permissions.permission').delete({
    where: { id: oldPerm.id },
  });
}
console.log(`🗑️  Removed ${oldPermissions.length} old permissions`);

// ✅ Создаём новые permissions
for (const perm of authenticatedPermissions) {
  await strapi.db.query('plugin::users-permissions.permission').create({
    data: {
      action: perm.action,
      role: authenticatedRole.id,
      // 🔑 КРИТИЧНО: Не указываем fields - это позволяет доступ ко ВСЕМ полям
    },
  });
  console.log(`✅ Created permission: ${perm.action} for authenticated`);
}
```

## 🚀 Тестирование

### Шаг 1: Перезапустить Strapi

```bash
# В терминале, где запущен Strapi:
# 1. Нажми Ctrl+C для остановки
# 2. Затем запусти снова:
cd /Users/zimbazo/WebstormProjects/Aetheris-community/backend/strapi-backend
npm run develop
```

**Ожидаемые логи в консоли Strapi**:

```
🔧 Setting up default permissions...
📋 Public role ID: 1
📋 Authenticated role ID: 2
🗑️  Removing old permissions for authenticated role...
🗑️  Removed XX old permissions
✅ Created permission: api::article.article.find for authenticated
✅ Created permission: api::article.article.findOne for authenticated
✅ Created permission: api::article.article.create for authenticated
✅ Created permission: api::article.article.update for authenticated
✅ Created permission: api::article.article.delete for authenticated
...
✅ Default permissions configured successfully
```

### Шаг 2: Проверить как НЕавторизованный пользователь

1. Разлогинься (если авторизован)
2. Открой `http://localhost:5173/`
3. **Ожидаемый результат**:
   - ✅ Статьи отображаются правильно
   - ✅ Есть заголовки, содержание, теги, автор

### Шаг 3: Проверить как авторизованный пользователь

1. Авторизуйся через Google OAuth
2. Открой `http://localhost:5173/`
3. **Ожидаемый результат**:
   - ✅ Статьи отображаются правильно
   - ✅ Есть заголовки, содержание, теги, автор
   - ✅ НЕТ пометки "draft"

### Шаг 4: Создать новую статью

1. Открой `http://localhost:5173/create-article`
2. Заполни:
   - **Заголовок**: "Финальная проверка авторизации"
   - **Содержание**: "Теперь должно работать!"
   - **Теги**: выбери 1-2 тега
3. Нажми **"Опубликовать"**

**Ожидаемый результат**:
- ✅ Статья появилась на главной
- ✅ Есть все поля (title, content, tags, author)
- ✅ Статья помечена как "published", а не "draft"

## 📊 Что было исправлено

| Проблема | Статус | Решение |
|----------|--------|---------|
| Пустые статьи для авторизованных | ✅ Исправлено | Пересозданы permissions с правильной структурой |
| Статьи помечены как draft | ✅ Исправлено | Permissions теперь дают доступ к полю `publishedAt` |
| Отсутствует `title` | ✅ Исправлено | Permissions дают доступ ко всем полям |
| Отсутствует `content` | ✅ Исправлено | Permissions дают доступ ко всем полям |
| Отсутствуют `tags` | ✅ Исправлено | Permissions дают доступ ко всем полям |

## 🔒 Безопасность

**Вопрос**: Безопасно ли давать доступ ко ВСЕМ полям для `authenticated` роли?

**Ответ**: ✅ **Да, безопасно**, потому что:

1. **Sensitive поля всё равно фильтруются**:
   - ❌ `password` (никогда не возвращается Strapi)
   - ❌ `email` (хранится хешированный, не включён в populate)
   - ❌ `resetPasswordToken` (не включён в populate)

2. **SECURITY проверки остались**:
   - ✅ `author` автоматически устанавливается из `ctx.state.user`
   - ✅ Проверка владельца при редактировании/удалении
   - ✅ JWT токен в HttpOnly cookies
   - ✅ CSRF защита
   - ✅ Rate limiting

3. **Permissions контролируют CRUD операции**:
   - ✅ `find` / `findOne` → чтение (безопасно для всех)
   - ✅ `create` → создание (только authenticated)
   - ✅ `update` → редактирование (только свои статьи, проверка в контроллере)
   - ✅ `delete` → удаление (только свои статьи, проверка в контроллере)

## 📝 Дополнительная информация

### Как работают permissions в Strapi v5

Permissions в Strapi имеют структуру:

```json
{
  "id": 123,
  "action": "api::article.article.find",
  "role": 2,
  "fields": null  // null = доступ ко ВСЕМ полям
}
```

**Важно**:
- `fields: null` → доступ ко **ВСЕМ** полям ✅
- `fields: []` → доступ к **НУЛЮ** полей ❌
- `fields: ["title", "content"]` → доступ только к этим полям

### Если нужно ограничить доступ к полям

Если когда-нибудь понадобится **ограничить доступ к определённым полям**, можно:

1. Зайти в Strapi Admin: `http://localhost:1337/admin`
2. Settings → Users & Permissions → Roles → Authenticated
3. Выбрать `Article` → нажать на `find`
4. Снять галочки с полей, к которым нужно запретить доступ

Например, можно запретить доступ к `likes_count` и `dislikes_count` для авторизованных пользователей.

## ✅ Готово!

После перезапуска Strapi статьи должны отображаться корректно **для всех пользователей** (и авторизованных, и неавторизованных)!

