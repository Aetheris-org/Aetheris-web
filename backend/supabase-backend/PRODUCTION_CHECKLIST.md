# 🚀 Чеклист для продакшена

## ⚠️ Критические проблемы (нужно исправить):

### 1. ❌ `include` не реализован
- **Проблема**: Resolvers используют `include: { author: true }`, но обертка Prisma не поддерживает связи
- **Где**: `article.ts`, `comment.ts`, `bookmark.ts`, `follow.ts`, `notification.ts`
- **Решение**: Нужно реализовать поддержку `include` через JOIN запросы в Supabase

### 2. ❌ `increment` не реализован
- **Проблема**: Используется `views: { increment: 1 }`, но обертка не поддерживает это
- **Где**: `article.ts:18`
- **Решение**: Нужно обрабатывать `increment`/`decrement` в методе `update`

### 3. ⚠️ Обработка ошибок
- Нужно добавить более детальную обработку ошибок Supabase
- Логирование всех ошибок БД

### 4. ⚠️ Переменные окружения для продакшена
- `NODE_ENV=production`
- `FRONTEND_URL` - URL фронтенда в продакшене
- `PUBLIC_URL` - публичный URL бэкенда
- Проверить все секреты (SESSION_SECRET, EMAIL_HMAC_SECRET)

### 5. ⚠️ Безопасность
- ✅ CORS настроен
- ✅ Helmet настроен
- ✅ Rate limiting настроен
- ⚠️ Проверить настройки для продакшена (более строгие лимиты)

### 6. ⚠️ Логирование
- ✅ Winston настроен
- ⚠️ Настроить ротацию логов для продакшена
- ⚠️ Настроить отправку критических ошибок (Sentry, etc.)

## ✅ Что уже работает:

- ✅ Express сервер
- ✅ Apollo Server
- ✅ Supabase подключение
- ✅ Базовые CRUD операции
- ✅ Middleware безопасности
- ✅ Миграции БД

## 📋 План действий:

1. **Исправить `include` поддержку** (критично)
2. **Исправить `increment` поддержку** (критично)
3. **Добавить обработку ошибок**
4. **Протестировать все GraphQL запросы**
5. **Настроить переменные окружения для продакшена**
6. **Настроить мониторинг и логирование**

## 🎯 После исправлений:

Бэкенд будет готов к продакшену!

