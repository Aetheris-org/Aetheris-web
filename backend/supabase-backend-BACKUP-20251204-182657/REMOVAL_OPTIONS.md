# 🗑️ Варианты удаления Express сервера

## 📋 Что находится в `backend/supabase-backend/`:

### ✅ Важно сохранить:
- `migrations/` - SQL миграции (нужны для БД)
- `scripts/` - полезные скрипты
- `*.md` - документация (инструкции, гайды)

### ❌ Можно удалить:
- `src/` - код Express + Apollo Server
- `node_modules/` - зависимости
- `package.json` - зависимости Express
- `tsconfig.json` - конфигурация TypeScript
- `prisma/` - Prisma схема (больше не используется)
- `logs/` - логи сервера
- `test-start.js` - тестовый скрипт

## 🎯 Варианты удаления:

### Вариант 1: Удалить только код (рекомендуется)

**Скрипт:** `scripts/remove-express-code.sh`

**Что делает:**
- ✅ Удаляет код (`src/`, `node_modules/`, `package.json`, etc.)
- ✅ Сохраняет миграции и документацию

**Плюсы:**
- Миграции остаются для справки
- Документация сохраняется
- Можно восстановить структуру БД

**Минусы:**
- Занимает немного места (миграции и документация)

### Вариант 2: Удалить всё (полное удаление)

**Скрипт:** `scripts/remove-express-server.sh`

**Что делает:**
- ❌ Удаляет всю директорию `backend/supabase-backend/`

**Плюсы:**
- Полная очистка
- Не занимает места

**Минусы:**
- Потеря миграций (но можно восстановить из git)
- Потеря документации

## 💡 Рекомендация:

**Вариант 1: Удалить только код**

Миграции могут быть полезны для:
- Справки по структуре БД
- Восстановления БД
- Документирования изменений

Документация полезна для:
- Понимания архитектуры
- Инструкций по настройке

## 🚀 Как использовать:

### Удалить только код:
```bash
cd backend/supabase-backend
bash scripts/remove-express-code.sh
```

### Удалить всё:
```bash
cd backend/supabase-backend
bash scripts/remove-express-server.sh
```

## ⚠️ Важно:

**Удаляйте только после успешного тестирования!**

Убедитесь, что:
- ✅ Все функции работают
- ✅ Нет ошибок
- ✅ Приложение полностью функционально

