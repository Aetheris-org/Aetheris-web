# 🔧 Настройка переменных окружения

## 📋 Шаг 1: Получить ключи Supabase

1. Откройте **Supabase Dashboard**: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → это `VITE_SUPABASE_URL`
   - **anon public** key → это `VITE_SUPABASE_ANON_KEY`

## 📋 Шаг 2: Создать файл .env.local

Создайте файл `frontend-react/.env.local`:

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

**Важно:**
- Файл `.env.local` не должен попадать в git (должен быть в `.gitignore`)
- Используйте `.env.local` для локальной разработки
- Для production используйте переменные окружения вашего хостинга

## 📋 Шаг 3: Установить зависимости

```bash
cd frontend-react
npm install
```

Это установит `@supabase/supabase-js` и другие зависимости.

## ✅ Проверка

После настройки проверьте:

1. Переменные окружения загружаются:
   ```typescript
   console.log(import.meta.env.VITE_SUPABASE_URL)
   ```

2. Supabase клиент создается без ошибок:
   - Откройте консоль браузера
   - Не должно быть ошибок о missing environment variables

3. Приложение запускается:
   ```bash
   npm run dev
   ```

## 🎯 Готово!

После этого приложение будет использовать Supabase REST API напрямую, без Express сервера.

