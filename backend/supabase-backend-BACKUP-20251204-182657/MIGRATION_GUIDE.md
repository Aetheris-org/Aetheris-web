# Руководство по миграции на новый бэкенд

## 🎯 Что изменилось

Старый бэкенд на KeystoneJS полностью заменен на новый бэкенд на основе:
- **Express** + **Apollo Server** (GraphQL)
- **Supabase** (PostgreSQL + Auth)
- **Prisma** (ORM)

## 📋 Шаги миграции

### 1. Установка зависимостей

```bash
cd backend/supabase-backend
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните все необходимые переменные:
- `SUPABASE_URL` - URL вашего Supabase проекта
- `SUPABASE_ANON_KEY` - Anon ключ
- `SUPABASE_SERVICE_ROLE_KEY` - Service Role ключ
- `DATABASE_URL` - PostgreSQL connection string

### 3. Применение миграций базы данных

#### Вариант A: Через Supabase Dashboard

1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте содержимое `migrations/001_initial_schema.sql`
4. Выполните SQL скрипт

#### Вариант B: Через Supabase CLI

```bash
supabase db push
```

### 4. Генерация Prisma Client

```bash
npm run prisma:generate
```

### 5. Запуск сервера

```bash
npm run dev
```

Сервер должен запуститься на `http://localhost:1337`
GraphQL API доступен на `http://localhost:1337/api/graphql`

## 🔄 Миграция данных (если нужно)

Если у вас уже есть данные в старом бэкенде, вам нужно:

1. Экспортировать данные из старой БД
2. Преобразовать формат данных (если нужно)
3. Импортировать в новую БД через Prisma или SQL

## 🔐 Аутентификация

Новый бэкенд использует Supabase Auth вместо KeystoneJS Auth. 

### Для фронтенда:

1. Используйте `@supabase/supabase-js` для аутентификации
2. Получайте JWT токен после входа
3. Передавайте токен в заголовке `Authorization: Bearer <token>` для всех GraphQL запросов

### Пример:

```typescript
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

// Вход
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'user@example.com',
  password: 'password',
});

// Использование токена в GraphQL запросах
const token = data.session.access_token;
```

## 📊 GraphQL API

### Endpoint

```
POST /api/graphql
```

### Основные отличия от KeystoneJS:

1. **Типы данных**: Все ID теперь могут быть как `String` (UUID для User), так и `Int` (для Article, Comment и т.д.)
2. **Аутентификация**: Через JWT токены Supabase вместо сессий KeystoneJS
3. **Валидация**: Встроенная валидация в resolvers, без отдельного слоя

### Примеры запросов

См. `README.md` для примеров GraphQL запросов.

## 🛠️ Отладка

### Проверка подключения к БД:

```bash
npm run prisma:studio
```

Откроется GUI для просмотра данных в БД.

### Логи:

Логи сохраняются в `logs/` директории:
- `application-YYYY-MM-DD.log` - общие логи
- `error-YYYY-MM-DD.log` - ошибки

## ⚠️ Важные замечания

1. **Старый бэкенд (keystonejs-backend)** можно удалить после успешной миграции
2. **Email хеширование**: В новом бэкенде email хранится в открытом виде (можно добавить хеширование позже, если нужно)
3. **RLS (Row Level Security)**: Настройте RLS политики в Supabase для дополнительной безопасности

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/`
2. Убедитесь, что все переменные окружения установлены
3. Проверьте подключение к Supabase
4. Убедитесь, что миграции применены

