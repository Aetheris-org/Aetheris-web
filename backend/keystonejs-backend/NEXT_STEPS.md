# Следующие шаги после миграций

## ✅ Выполнено
- [x] Миграции базы данных выполнены

## 📋 Что делать дальше (по порядку)

### Шаг 1: Установить зависимости

```bash
cd backend/keystonejs-backend
npm install
```

Это установит `@supabase/supabase-js`, который мы добавили в `package.json`.

---

### Шаг 2: Настроить переменные окружения

Создайте файл `.env` в `backend/keystonejs-backend/` (если его еще нет):

```bash
# Скопируйте пример
cp .env.example .env
```

Заполните следующие переменные:

```bash
# ============================================
# Supabase Configuration (ОБЯЗАТЕЛЬНО)
# ============================================
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
SUPABASE_DATABASE_URL=postgresql://postgres:[ПАРОЛЬ]@db.[PROJECT-REF].supabase.co:5432/postgres

# Для обратной совместимости
DATABASE_URL=${SUPABASE_DATABASE_URL}

# ============================================
# Authentication (ОБЯЗАТЕЛЬНО)
# ============================================
# Сгенерируйте секреты:
# openssl rand -base64 64
SESSION_SECRET=ваш-секрет-минимум-32-символа
EMAIL_HMAC_SECRET=ваш-секрет-минимум-32-символа

# ============================================
# Application URLs
# ============================================
FRONTEND_URL=http://localhost:5173
PUBLIC_URL=http://localhost:1337
PORT=1337
NODE_ENV=development
```

**Где взять ключи Supabase:**
1. Откройте Supabase Dashboard
2. Settings > API
3. Скопируйте:
   - **Project URL** → `SUPABASE_URL`
   - **anon public** → `SUPABASE_ANON_KEY`
   - **service_role secret** → `SUPABASE_SERVICE_ROLE_KEY`
4. Settings > Database > Connection string (URI) → `SUPABASE_DATABASE_URL`

---

### Шаг 3: Настроить EMAIL_HMAC_SECRET в Supabase

Это нужно для работы хеширования email. Выполните в Supabase SQL Editor:

```sql
ALTER DATABASE postgres SET app.settings.email_hmac_secret = 'ваш-email-hmac-secret-здесь';
```

**Важно**: Используйте тот же секрет, что и в `.env` файле для `EMAIL_HMAC_SECRET`!

---

### Шаг 4: Настроить Supabase Auth

В Supabase Dashboard:

1. **Authentication > Providers**:
   - Включите **Email** (если нужна регистрация через email/password)
   - Включите **Google** (если нужен OAuth)
   - Настройте OAuth credentials для Google (если используете)

2. **Authentication > URL Configuration**:
   - Добавьте **Redirect URLs**:
     - `http://localhost:5173/auth/callback` (development)
     - `http://localhost:1337/api/connect/google/callback` (для текущего OAuth flow)

3. **Settings > API**:
   - Убедитесь, что **Row Level Security (RLS)** включен

---

### Шаг 5: Проверить подключение

Запустите backend:

```bash
cd backend/keystonejs-backend
npm run dev
```

**Что должно произойти:**
- ✅ Backend запускается без ошибок
- ✅ В логах видно: "✅ Supabase client configured successfully"
- ✅ В логах видно: "✅ Supabase configuration detected - using Supabase PostgreSQL"
- ✅ GraphQL API доступен на `http://localhost:1337/api/graphql`

**Если есть ошибки:**
- Проверьте переменные окружения в `.env`
- Проверьте, что `SUPABASE_DATABASE_URL` правильный
- Проверьте, что миграции выполнены успешно

---

### Шаг 6: Проверить работу

#### 6.1. Проверка таблиц

В Supabase Dashboard > Table Editor должны быть видны все таблицы:
- `profiles`
- `articles`
- `comments`
- `article_reactions`
- `comment_reactions`
- `bookmarks`
- `follows`
- `notifications`

#### 6.2. Проверка GraphQL API

Откройте в браузере:
```
http://localhost:1337/api/graphql
```

Должен открыться GraphQL Playground (в development режиме).

Попробуйте выполнить простой запрос:

```graphql
query {
  __typename
}
```

Должен вернуться результат.

#### 6.3. Проверка триггеров (опционально)

В Supabase SQL Editor выполните:

```sql
-- Проверка триггеров
SELECT tgname, tgrelid::regclass 
FROM pg_trigger 
WHERE tgname IN ('on_auth_user_created', 'on_auth_user_updated', 'update_profiles_updated_at');
```

Должны быть видны все триггеры.

---

### Шаг 7: Тестирование аутентификации (опционально)

Если все работает, можно протестировать:

1. **Создание пользователя через Supabase Auth** (если настроен)
2. **Проверка автоматического создания профиля** (триггер должен создать запись в `public.profiles`)

---

## 🚨 Частые проблемы

### Ошибка подключения к БД

```
Error: connect ECONNREFUSED
```

**Решение:**
- Проверьте `SUPABASE_DATABASE_URL`
- Убедитесь, что IP адрес разрешен в Supabase Dashboard > Settings > Database > Connection Pooling

### Ошибка "EMAIL_HMAC_SECRET not configured"

**Решение:**
- Выполните шаг 3 (настройка EMAIL_HMAC_SECRET в Supabase)
- Убедитесь, что секрет одинаковый в `.env` и в Supabase

### Ошибка "Supabase client is not configured"

**Решение:**
- Проверьте переменные окружения в `.env`
- Убедитесь, что файл `.env` находится в `backend/keystonejs-backend/`
- Перезапустите backend после изменения `.env`

---

## ✅ Готово!

После выполнения всех шагов ваш проект должен:
- ✅ Подключаться к Supabase PostgreSQL
- ✅ Использовать Supabase Auth для аутентификации
- ✅ Автоматически создавать профили при регистрации
- ✅ Работать с Row Level Security

**Следующий этап:** Обновление frontend для работы с Supabase Auth (если нужно).


