# Как найти SUPABASE_URL и SUPABASE_DATABASE_URL

Подробная пошаговая инструкция с точными местами в интерфейсе.

## 🔍 Шаг 1: Найти SUPABASE_URL

### Вариант 1: Через Settings > API

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. В **левом меню** нажмите на **Settings** (⚙️ Настройки)
4. В подменю выберите **API**
5. На странице найдите секцию **Project URL**
6. Скопируйте URL - это ваш `SUPABASE_URL`

**Должен выглядеть так:**
```
https://lublvnvoawndnmkgndct.supabase.co
```

### Вариант 2: Через Project Settings

1. В левом верхнем углу нажмите на название проекта
2. Выберите **Project Settings**
3. Найдите **Reference ID** или **Project URL**
4. URL будет в формате: `https://[reference-id].supabase.co`

### Вариант 3: Из ваших ключей (быстрый способ)

Судя по вашему ref в ключах (`lublvnvoawndnmkgndct`), ваш URL должен быть:

```
https://lublvnvoawndnmkgndct.supabase.co
```

**Проверьте этот URL** - он должен открываться в браузере и показывать страницу Supabase.

---

## 🗄️ Шаг 2: Найти SUPABASE_DATABASE_URL

Это самый важный и часто сложный шаг. Следуйте точно:

### Шаг 2.1: Откройте настройки Database

1. В Supabase Dashboard
2. В **левом меню** нажмите на **Settings** (⚙️)
3. В подменю выберите **Database**

### Шаг 2.2: Найдите Connection string

На странице **Settings > Database** прокрутите вниз до секции **Connection string**.

Вы увидите несколько вкладок:
- **URI** ← **ВЫБЕРИТЕ ЭТУ**
- **JDBC**
- **Golang**
- и т.д.

### Шаг 2.3: Скопируйте URI

1. Нажмите на вкладку **URI**
2. Вы увидите строку вида:
   ```
   postgresql://postgres.[ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres
   ```
   или
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.[ref].supabase.co:5432/postgres
   ```

3. **Скопируйте эту строку**

### Шаг 2.4: Замените пароль

В строке будет `[YOUR-PASSWORD]` или `[password]` - нужно заменить на реальный пароль.

#### Если знаете пароль:
- Просто замените `[YOUR-PASSWORD]` на ваш пароль

#### Если НЕ знаете пароль:

1. На той же странице **Settings > Database**
2. Прокрутите до секции **Database password**
3. Если пароль не установлен или забыт:
   - Нажмите кнопку **Reset database password**
   - Сохраните новый пароль в безопасном месте!
   - Используйте этот пароль в connection string

### Шаг 2.5: Финальная строка

После замены пароля у вас должна получиться строка вида:

```
postgresql://postgres:ваш-пароль@db.lublvnvoawndnmkgndct.supabase.co:5432/postgres
```

**Это ваш `SUPABASE_DATABASE_URL`**

---

## 📝 Альтернативный способ: Получить через Connection Pooling

Если не можете найти обычный connection string:

1. Settings > Database
2. Найдите секцию **Connection Pooling**
3. Там будет **Connection string** для pooling
4. Используйте его, но замените порт на `5432` (если там другой)

---

## 🎯 Визуальное описание интерфейса

### Settings > API страница:

```
┌─────────────────────────────────────────┐
│ ← Settings (в левом меню)               │
│   ├─ General                            │
│   ├─ API ← НАЖМИТЕ СЮДА                 │
│   ├─ Database                           │
│   └─ ...                                │
└─────────────────────────────────────────┘

После нажатия на API:

┌─────────────────────────────────────────┐
│ Settings > API                          │
├─────────────────────────────────────────┤
│                                         │
│ Project URL                             │
│ ┌─────────────────────────────────────┐ │
│ │ https://lublvnvoawndnmkgndct...     │ │ ← СКОПИРУЙТЕ ЭТО
│ └─────────────────────────────────────┘ │
│                                         │
│ Project API keys                        │
│ [ваши ключи]                            │
└─────────────────────────────────────────┘
```

### Settings > Database страница:

```
┌─────────────────────────────────────────┐
│ ← Settings (в левом меню)               │
│   ├─ General                            │
│   ├─ API                                │
│   ├─ Database ← НАЖМИТЕ СЮДА            │
│   └─ ...                                │
└─────────────────────────────────────────┘

После нажатия на Database:

┌─────────────────────────────────────────┐
│ Settings > Database                    │
├─────────────────────────────────────────┤
│ ...                                     │
│                                         │
│ Connection string                       │
│ ┌─────────────────────────────────────┐ │
│ │ [URI] [JDBC] [Golang] ...           │ │
│ │                                     │ │
│ │ postgresql://postgres:[PASSWORD]@... │ │ ← СКОПИРУЙТЕ ЭТО
│ └─────────────────────────────────────┘ │
│                                         │
│ Database password                       │
│ [Reset database password] ← если нужно │
└─────────────────────────────────────────┘
```

---

## ✅ Быстрая проверка

После получения обоих значений, проверьте:

1. **SUPABASE_URL** должен открываться в браузере
   - Попробуйте: `https://lublvnvoawndnmkgndct.supabase.co`
   - Должна открыться страница Supabase

2. **SUPABASE_DATABASE_URL** должен содержать:
   - `postgresql://` в начале
   - `postgres:` после `://`
   - Ваш пароль (не `[YOUR-PASSWORD]`)
   - `@db.` или `@aws-0-`
   - `.supabase.co` или `.pooler.supabase.com`
   - `:5432` или `:6543` (порт)
   - `/postgres` в конце

---

## 🆘 Если все еще не можете найти

### Для SUPABASE_URL:

Попробуйте этот URL (на основе вашего ref):
```
https://lublvnvoawndnmkgndct.supabase.co
```

Если он открывается - это ваш URL!

### Для SUPABASE_DATABASE_URL:

Если не видите Connection string:

1. Попробуйте найти в **Settings > Database > Connection Pooling**
2. Или используйте формат:
   ```
   postgresql://postgres:ВАШ-ПАРОЛЬ@db.lublvnvoawndnmkgndct.supabase.co:5432/postgres
   ```
   (замените `ВАШ-ПАРОЛЬ` на пароль из "Reset database password")

---

## 📋 Итоговый чеклист

- [ ] SUPABASE_URL найден (или используйте `https://lublvnvoawndnmkgndct.supabase.co`)
- [ ] SUPABASE_DATABASE_URL найден (с реальным паролем)
- [ ] Пароль базы данных известен (или сброшен через "Reset database password")
- [ ] Оба значения скопированы

После этого можно заполнять `.env` файл!


