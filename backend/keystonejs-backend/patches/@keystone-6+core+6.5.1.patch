diff --git a/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.dev.js b/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.dev.js
index 14aec6f..2c20cad 100644
--- a/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.dev.js
+++ b/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.dev.js
@@ -299,6 +299,9 @@ const nextConfigTemplate = basePath => `const nextConfig = {
     // as they import ts files into nextjs
     transpilePackages: ['../../admin'],
     ${basePath ? `basePath: '${basePath}',` : ''}
+    // Отключаем статический экспорт - Admin UI должен работать только в runtime
+    // Это исправляет ошибку "Html should not be imported outside of pages/_document"
+    output: 'standalone',
   }
   
   module.exports = nextConfig`;
@@ -318,6 +321,10 @@ function writeAdminFiles(config, graphQLSchema, adminMeta, configFileExists) {
     mode: 'write',
     src: noAccessTemplate(config.session),
     outputPath: 'pages/no-access.js'
+  }, {
+    mode: 'write',
+    src: "import { Html, Head, Main, NextScript } from 'next/document'\n\nexport default function Document() {\n  return (\n    <Html>\n      <Head />\n      <body>\n        <Main />\n        <NextScript />\n      </body>\n    </Html>\n  )\n}\n",
+    outputPath: 'pages/_document.tsx'
   }, {
     mode: 'write',
     src: appTemplate(adminMeta, graphQLSchema, {
@@ -548,7 +555,30 @@ async function build(cwd, {
   const paths = system.getPaths(cwd);
   await generateAdminUI(system.config, system.graphQLSchema, system.adminMeta, paths.admin, false);
   console.log('✨ Building Admin UI');
-  await nextBuild__default["default"](paths.admin, undefined, undefined, undefined, undefined, undefined, undefined, 'default');
+  // Перехватываем процесс билда, чтобы отключить статический экспорт
+  // Временно переименовываем export/index.js, чтобы Next.js не мог его использовать
+  const exportPath = require.resolve('next/dist/export/index.js');
+  const exportPathBackup = exportPath + '.backup';
+  const fs = require('fs');
+  let exportBackedUp = false;
+  try {
+    if (fs.existsSync(exportPath) && !fs.existsSync(exportPathBackup)) {
+      fs.renameSync(exportPath, exportPathBackup);
+      // Создаем mock-функцию, которая ничего не делает (standalone режим не требует экспорта)
+      fs.writeFileSync(exportPath, `module.exports = {
+        default: async () => { /* Standalone mode - export disabled */ },
+        exportAppImpl: async () => { /* Standalone mode - export disabled */ },
+        ExportError: class ExportError extends Error {}
+      };`);
+      exportBackedUp = true;
+    }
+    await nextBuild__default["default"](paths.admin, undefined, undefined, undefined, undefined, undefined, undefined, 'default');
+  } finally {
+    // Восстанавливаем оригинальный файл
+    if (exportBackedUp && fs.existsSync(exportPathBackup)) {
+      fs.renameSync(exportPathBackup, exportPath);
+    }
+  }
 }
 
 // prompts is badly typed so we have some more specific typed APIs
diff --git a/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.prod.js b/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.prod.js
index 90fba3e..45c6420 100644
--- a/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.prod.js
+++ b/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.cjs.prod.js
@@ -299,6 +299,9 @@ const nextConfigTemplate = basePath => `const nextConfig = {
     // as they import ts files into nextjs
     transpilePackages: ['../../admin'],
     ${basePath ? `basePath: '${basePath}',` : ''}
+    // Отключаем статический экспорт - Admin UI должен работать только в runtime
+    // Это исправляет ошибку "Html should not be imported outside of pages/_document"
+    output: 'standalone',
   }
   
   module.exports = nextConfig`;
@@ -318,6 +321,10 @@ function writeAdminFiles(config, graphQLSchema, adminMeta, configFileExists) {
     mode: 'write',
     src: noAccessTemplate(config.session),
     outputPath: 'pages/no-access.js'
+  }, {
+    mode: 'write',
+    src: "import { Html, Head, Main, NextScript } from 'next/document'\n\nexport default function Document() {\n  return (\n    <Html>\n      <Head />\n      <body>\n        <Main />\n        <NextScript />\n      </body>\n    </Html>\n  )\n}\n",
+    outputPath: 'pages/_document.tsx'
   }, {
     mode: 'write',
     src: appTemplate(adminMeta, graphQLSchema, {
@@ -548,7 +555,30 @@ async function build(cwd, {
   const paths = system.getPaths(cwd);
   await generateAdminUI(system.config, system.graphQLSchema, system.adminMeta, paths.admin, false);
   console.log('✨ Building Admin UI');
-  await nextBuild__default["default"](paths.admin, undefined, undefined, undefined, undefined, undefined, undefined, 'default');
+  // Перехватываем процесс билда, чтобы отключить статический экспорт
+  // Временно переименовываем export/index.js, чтобы Next.js не мог его использовать
+  const exportPath = require.resolve('next/dist/export/index.js');
+  const exportPathBackup = exportPath + '.backup';
+  const fs = require('fs');
+  let exportBackedUp = false;
+  try {
+    if (fs.existsSync(exportPath) && !fs.existsSync(exportPathBackup)) {
+      fs.renameSync(exportPath, exportPathBackup);
+      // Создаем mock-функцию, которая ничего не делает (standalone режим не требует экспорта)
+      fs.writeFileSync(exportPath, `module.exports = {
+        default: async () => { /* Standalone mode - export disabled */ },
+        exportAppImpl: async () => { /* Standalone mode - export disabled */ },
+        ExportError: class ExportError extends Error {}
+      };`);
+      exportBackedUp = true;
+    }
+    await nextBuild__default["default"](paths.admin, undefined, undefined, undefined, undefined, undefined, undefined, 'default');
+  } finally {
+    // Восстанавливаем оригинальный файл
+    if (exportBackedUp && fs.existsSync(exportPathBackup)) {
+      fs.renameSync(exportPathBackup, exportPath);
+    }
+  }
 }
 
 // prompts is badly typed so we have some more specific typed APIs
diff --git a/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.esm.js b/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.esm.js
index cf7732f..640726f 100644
--- a/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.esm.js
+++ b/node_modules/@keystone-6/core/scripts/cli/dist/keystone-6-core-scripts-cli.esm.js
@@ -257,6 +257,13 @@ const nextConfigTemplate = basePath => `const nextConfig = {
     // as they import ts files into nextjs
     transpilePackages: ['../../admin'],
     ${basePath ? `basePath: '${basePath}',` : ''}
+    // Отключаем статический экспорт и prerendering - Admin UI должен работать только в runtime
+    // Это исправляет ошибку "Html should not be imported outside of pages/_document"
+    output: 'standalone',
+    // Отключаем автоматический статический экспорт
+    generateBuildId: async () => {
+      return 'build-' + Date.now()
+    },
   }
   
   module.exports = nextConfig`;
@@ -276,6 +283,10 @@ function writeAdminFiles(config, graphQLSchema, adminMeta, configFileExists) {
     mode: 'write',
     src: noAccessTemplate(config.session),
     outputPath: 'pages/no-access.js'
+  }, {
+    mode: 'write',
+    src: "import { Html, Head, Main, NextScript } from 'next/document'\n\nexport default function Document() {\n  return (\n    <Html>\n      <Head />\n      <body>\n        <Main />\n        <NextScript />\n      </body>\n    </Html>\n  )\n}\n",
+    outputPath: 'pages/_document.tsx'
   }, {
     mode: 'write',
     src: appTemplate(adminMeta, graphQLSchema, {
@@ -506,7 +517,40 @@ async function build(cwd, {
   const paths = system.getPaths(cwd);
   await generateAdminUI(system.config, system.graphQLSchema, system.adminMeta, paths.admin, false);
   console.log('✨ Building Admin UI');
-  await nextBuild(paths.admin, undefined, undefined, undefined, undefined, undefined, undefined, 'default');
+  // Перехватываем процесс билда, чтобы отключить статический экспорт
+  // Временно переименовываем export/index.js, чтобы Next.js не мог его использовать
+  const { createRequire } = await import('node:module');
+  const require = createRequire(import.meta.url);
+  const exportPath = require.resolve('next/dist/export/index.js');
+  const exportPathBackup = exportPath + '.backup';
+  const fs = await import('node:fs/promises');
+  let exportBackedUp = false;
+  try {
+    try {
+      await fs.access(exportPath);
+      try {
+        await fs.access(exportPathBackup);
+      } catch {
+        await fs.rename(exportPath, exportPathBackup);
+        // Создаем mock-функцию, которая ничего не делает (standalone режим не требует экспорта)
+        await fs.writeFile(exportPath, `module.exports = {
+          default: async () => { /* Standalone mode - export disabled */ },
+          exportAppImpl: async () => { /* Standalone mode - export disabled */ },
+          ExportError: class ExportError extends Error {}
+        };`);
+        exportBackedUp = true;
+      }
+    } catch {}
+    await nextBuild(paths.admin, undefined, undefined, undefined, undefined, undefined, undefined, 'default');
+  } finally {
+    // Восстанавливаем оригинальный файл
+    if (exportBackedUp) {
+      try {
+        await fs.access(exportPathBackup);
+        await fs.rename(exportPathBackup, exportPath);
+      } catch {}
+    }
+  }
 }
 
 // prompts is badly typed so we have some more specific typed APIs
diff --git a/node_modules/@keystone-6/core/src/admin-ui/templates/document.ts b/node_modules/@keystone-6/core/src/admin-ui/templates/document.ts
new file mode 100644
index 0000000..8b0a809
--- /dev/null
+++ b/node_modules/@keystone-6/core/src/admin-ui/templates/document.ts
@@ -0,0 +1,15 @@
+export const documentTemplate = `import { Html, Head, Main, NextScript } from 'next/document'
+
+export default function Document() {
+  return (
+    <Html>
+      <Head />
+      <body>
+        <Main />
+        <NextScript />
+      </body>
+    </Html>
+  )
+}
+`
+
diff --git a/node_modules/@keystone-6/core/src/admin-ui/templates/index.ts b/node_modules/@keystone-6/core/src/admin-ui/templates/index.ts
index eaf284d..3288a0d 100644
--- a/node_modules/@keystone-6/core/src/admin-ui/templates/index.ts
+++ b/node_modules/@keystone-6/core/src/admin-ui/templates/index.ts
@@ -11,6 +11,7 @@ import { itemTemplate } from './item'
 import { noAccessTemplate } from './no-access'
 import { createItemTemplate } from './create-item'
 import { nextConfigTemplate } from './next-config'
+import { documentTemplate } from './document'
 
 const pkgDir = Path.dirname(require.resolve('@keystone-6/core/package.json'))
 
@@ -31,6 +32,11 @@ export function writeAdminFiles (config: __ResolvedKeystoneConfig,
       outputPath: 'public/favicon.ico',
     },
     { mode: 'write' as const, src: noAccessTemplate(config.session), outputPath: 'pages/no-access.js' },
+    {
+      mode: 'write' as const,
+      src: documentTemplate,
+      outputPath: 'pages/_document.tsx',
+    },
     {
       mode: 'write' as const,
       src: appTemplate(
diff --git a/node_modules/@keystone-6/core/src/admin-ui/templates/next-config.ts b/node_modules/@keystone-6/core/src/admin-ui/templates/next-config.ts
index 9336e41..2e08709 100644
--- a/node_modules/@keystone-6/core/src/admin-ui/templates/next-config.ts
+++ b/node_modules/@keystone-6/core/src/admin-ui/templates/next-config.ts
@@ -14,6 +14,9 @@ export const nextConfigTemplate = (basePath?: string) =>
     // as they import ts files into nextjs
     transpilePackages: ['../../admin'],
     ${basePath ? `basePath: '${basePath}',` : ''}
+    // Отключаем статический экспорт - Admin UI должен работать только в runtime
+    // Это исправляет ошибку "Html should not be imported outside of pages/_document"
+    output: 'standalone',
   }
   
   module.exports = nextConfig`
