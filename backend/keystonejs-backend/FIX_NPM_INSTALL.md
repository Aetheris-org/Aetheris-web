# Решение проблемы с npm install (Prisma engines)

Ошибка `ECONNRESET` при установке Prisma engines обычно связана с проблемами сети или кэша.

## 🔧 Решения (попробуйте по порядку)

### Решение 1: Повторите установку

Иногда помогает просто повторить команду:

```bash
cd backend/keystonejs-backend
npm install
```

---

### Решение 2: Очистите кэш и повторите

```bash
cd backend/keystonejs-backend

# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Повторите установку
npm install
```

---

### Решение 3: Установите Prisma engines вручную

```bash
cd backend/keystonejs-backend

# Установите зависимости без Prisma
npm install --ignore-scripts

# Затем установите Prisma engines отдельно
npx prisma generate
```

---

### Решение 4: Используйте другой реестр (если проблема с сетью)

Если у вас проблемы с доступом к npm registry:

```bash
# Используйте альтернативный реестр
npm install --registry https://registry.npmmirror.com

# Или установите через yarn
yarn install
```

---

### Решение 5: Установите Prisma через переменные окружения

```bash
cd backend/keystonejs-backend

# Установите переменную для Prisma
export PRISMA_ENGINES_MIRROR=https://binaries.prisma.sh

# Или используйте альтернативный mirror
export PRISMA_ENGINES_MIRROR=https://npmmirror.com/mirrors/prisma

# Затем установите
npm install
```

---

### Решение 6: Пропустите postinstall скрипты

Если проблема только с Prisma postinstall:

```bash
cd backend/keystonejs-backend

# Установите без postinstall скриптов
npm install --ignore-scripts

# Затем сгенерируйте Prisma client вручную
npx prisma generate
```

---

## ✅ Рекомендуемая последовательность

Попробуйте в таком порядке:

```bash
cd backend/keystonejs-backend

# 1. Очистите кэш
npm cache clean --force

# 2. Удалите старые файлы
rm -rf node_modules package-lock.json

# 3. Установите заново
npm install

# Если не помогло, попробуйте с игнорированием скриптов
npm install --ignore-scripts
npx prisma generate
```

---

## 🔍 Если ничего не помогает

### Вариант A: Используйте yarn вместо npm

```bash
cd backend/keystonejs-backend

# Установите yarn (если еще не установлен)
npm install -g yarn

# Установите зависимости через yarn
yarn install
```

### Вариант B: Проверьте сеть и прокси

Если вы за прокси или файрволом:

```bash
# Настройте npm для работы с прокси
npm config set proxy http://your-proxy:port
npm config set https-proxy http://your-proxy:port

# Или отключите прокси для npm
npm config delete proxy
npm config delete https-proxy
```

---

## 📝 После успешной установки

Когда `npm install` завершится успешно:

1. Проверьте, что `@supabase/supabase-js` установлен:
   ```bash
   npm list @supabase/supabase-js
   ```

2. Запустите backend:
   ```bash
   npm run dev
   ```

---

## 🆘 Если проблема сохраняется

Если ни одно решение не помогло, можно временно продолжить без Prisma engines (если они не критичны для текущей задачи):

```bash
# Установите все кроме Prisma
npm install --ignore-scripts

# Backend должен работать, но некоторые функции Prisma могут быть недоступны
```

Но лучше решить проблему с Prisma, так как KeystoneJS использует Prisma для работы с БД.


