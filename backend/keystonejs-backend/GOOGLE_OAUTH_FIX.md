# 🔧 Исправление ошибки redirect_uri_mismatch

## Проблема
Google возвращает ошибку `redirect_uri_mismatch`, что означает, что redirect URI в запросе не совпадает с настройками в Google Console.

## Решение

### 1. Откройте Google Cloud Console

Перейдите по адресу: https://console.cloud.google.com/

### 2. Найдите ваш OAuth 2.0 Client ID

1. Перейдите в **APIs & Services** → **Credentials**
2. Найдите ваш OAuth 2.0 Client ID (с ID: `1057500911445-h0ss5gbn1ml02j871c2dajgtot2ls48m`)
3. Нажмите на него для редактирования

### 3. Добавьте Authorized redirect URIs

В разделе **"Authorized redirect URIs"** добавьте **ТОЧНО** такой URL:

```
http://localhost:1337/api/connect/google/callback
```

**ВАЖНО:**
- ✅ Используйте `http://` (не `https://`)
- ✅ Порт должен быть `1337` (не `5173`)
- ✅ Путь должен быть `/api/connect/google/callback` (полный путь)
- ✅ Не должно быть пробелов в начале или конце
- ✅ Не должно быть слэша в конце (`/`)

### 4. Сохраните изменения

Нажмите **"Save"** внизу страницы.

### 5. Подождите несколько секунд

Изменения в Google Console могут вступить в силу не сразу (обычно 1-2 минуты).

### 6. Попробуйте снова

Перезапустите backend (если нужно) и попробуйте авторизоваться снова.

## Проверка текущих настроек

В вашем `.env` файле должно быть:

```env
GOOGLE_CALLBACK_URL=http://localhost:1337/api/connect/google/callback
```

Этот URL должен **ТОЧНО** совпадать с тем, что указано в Google Console.

## Если ошибка все еще возникает

1. **Проверьте, что URL совпадает точно:**
   - Откройте Google Console
   - Скопируйте URL из "Authorized redirect URIs"
   - Сравните с `GOOGLE_CALLBACK_URL` в `.env`
   - Они должны быть идентичны (включая регистр, пробелы, слэши)

2. **Проверьте, что изменения сохранены:**
   - В Google Console нажмите "Save"
   - Подождите 1-2 минуты

3. **Проверьте, что backend использует правильный URL:**
   - Убедитесь, что `.env` файл загружается правильно
   - Перезапустите backend после изменения `.env`

4. **Проверьте логи backend:**
   - При запуске backend должен логировать используемый `GOOGLE_CALLBACK_URL`
   - Убедитесь, что он правильный

## Для production

Когда будете деплоить в production, добавьте также production URL:

```
https://your-domain.com/api/connect/google/callback
```

И обновите `.env`:

```env
GOOGLE_CALLBACK_URL=https://your-domain.com/api/connect/google/callback
```

