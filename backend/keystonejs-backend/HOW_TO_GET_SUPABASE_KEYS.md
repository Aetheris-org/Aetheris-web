# Как получить ключи Supabase для переменных окружения

Подробная инструкция, где найти все необходимые ключи в Supabase Dashboard.

## 📍 Шаг 1: Войдите в Supabase Dashboard

1. Откройте [https://app.supabase.com](https://app.supabase.com)
2. Войдите в свой аккаунт
3. Выберите ваш проект (или создайте новый, если еще не создали)

---

## 🔑 Шаг 2: Получение ключей API

### 2.1. Откройте настройки API

1. В левом меню нажмите на **Settings** (⚙️ Настройки)
2. Выберите **API** в подменю

### 2.2. Найдите нужные ключи

На странице **Settings > API** вы увидите несколько секций:

#### ✅ SUPABASE_URL (Project URL)

**Где найти:**
- В секции **Project URL**
- Это будет что-то вроде: `https://xxxxxxxxxxxxx.supabase.co`

**Скопируйте это значение** → это ваш `SUPABASE_URL`

---

#### ✅ SUPABASE_ANON_KEY (anon public)

**Где найти:**
- В секции **Project API keys**
- Найдите ключ с типом **anon** и scope **public**
- Это длинная строка, начинающаяся с `eyJ...`

**Скопируйте это значение** → это ваш `SUPABASE_ANON_KEY`

**⚠️ Важно:** Этот ключ можно использовать на frontend (публичный ключ)

---

#### ✅ SUPABASE_SERVICE_ROLE_KEY (service_role secret)

**Где найти:**
- В той же секции **Project API keys**
- Найдите ключ с типом **service_role** и scope **secret**
- Это тоже длинная строка, начинающаяся с `eyJ...`

**Скопируйте это значение** → это ваш `SUPABASE_SERVICE_ROLE_KEY`

**🔒 КРИТИЧЕСКИ ВАЖНО:** 
- Этот ключ **НИКОГДА** не используйте на frontend!
- Он дает полный доступ к базе данных, обходя RLS
- Используйте только на backend сервере
- Не коммитьте в Git!

---

## 🗄️ Шаг 3: Получение Database URL

### 3.1. Откройте настройки Database

1. В левом меню нажмите на **Settings** (⚙️ Настройки)
2. Выберите **Database** в подменю

### 3.2. Найдите Connection string

На странице **Settings > Database** найдите секцию **Connection string**

#### Вариант 1: URI (рекомендуется)

**Где найти:**
- В секции **Connection string**
- Выберите вкладку **URI**
- Вы увидите строку вида:
  ```
  postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
  ```

**Что нужно сделать:**
1. Скопируйте эту строку
2. Замените `[YOUR-PASSWORD]` на ваш реальный пароль базы данных
3. Если не знаете пароль:
   - Прокрутите страницу вниз
   - Найдите секцию **Database password**
   - Если пароль не установлен, нажмите **Reset database password**
   - Сохраните новый пароль в безопасном месте!

**Скопируйте полную строку с паролем** → это ваш `SUPABASE_DATABASE_URL`

**Пример:**
```
postgresql://postgres:MySecurePassword123@db.abcdefghijklmnop.supabase.co:5432/postgres
```

---

## 📝 Шаг 4: Создание .env файла

Теперь создайте файл `.env` в папке `backend/keystonejs-backend/`:

### 4.1. Создайте файл

```bash
cd backend/keystonejs-backend
touch .env
# или
cp .env.example .env
```

### 4.2. Заполните переменные

Откройте файл `.env` и вставьте ваши ключи:

```bash
# ============================================
# Supabase Configuration
# ============================================
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ваш-anon-key-здесь
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ваш-service-role-key-здесь
SUPABASE_DATABASE_URL=postgresql://postgres:ваш-пароль@db.xxxxx.supabase.co:5432/postgres

# Для обратной совместимости
DATABASE_URL=${SUPABASE_DATABASE_URL}

# ============================================
# Authentication Secrets
# ============================================
# Сгенерируйте секреты командой: openssl rand -base64 64
SESSION_SECRET=ваш-секрет-минимум-32-символа-здесь
EMAIL_HMAC_SECRET=ваш-секрет-минимум-32-символа-здесь

# ============================================
# Application URLs
# ============================================
FRONTEND_URL=http://localhost:5173
PUBLIC_URL=http://localhost:1337
PORT=1337
NODE_ENV=development
```

---

## 🔐 Шаг 5: Генерация секретов

Для `SESSION_SECRET` и `EMAIL_HMAC_SECRET` нужно сгенерировать криптографически стойкие секреты.

### 5.1. Генерация через OpenSSL

```bash
# Генерируем SESSION_SECRET
openssl rand -base64 64

# Генерируем EMAIL_HMAC_SECRET
openssl rand -base64 64
```

### 5.2. Или через Node.js

```bash
node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
```

Выполните команду дважды и скопируйте результаты в `.env` файл.

---

## ✅ Шаг 6: Проверка

После заполнения `.env` файла проверьте:

1. **Все переменные заполнены** (нет пустых значений)
2. **SUPABASE_URL** начинается с `https://`
3. **SUPABASE_ANON_KEY** и **SUPABASE_SERVICE_ROLE_KEY** начинаются с `eyJ`
4. **SUPABASE_DATABASE_URL** содержит пароль (не `[YOUR-PASSWORD]`)
5. **SESSION_SECRET** и **EMAIL_HMAC_SECRET** минимум 32 символа

---

## 🎯 Быстрая шпаргалка

| Переменная | Где найти | Пример |
|------------|-----------|--------|
| `SUPABASE_URL` | Settings > API > Project URL | `https://xxx.supabase.co` |
| `SUPABASE_ANON_KEY` | Settings > API > anon public | `eyJhbGc...` |
| `SUPABASE_SERVICE_ROLE_KEY` | Settings > API > service_role secret | `eyJhbGc...` |
| `SUPABASE_DATABASE_URL` | Settings > Database > Connection string (URI) | `postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres` |
| `SESSION_SECRET` | Генерировать: `openssl rand -base64 64` | `abc123...` |
| `EMAIL_HMAC_SECRET` | Генерировать: `openssl rand -base64 64` | `xyz789...` |

---

## 🚨 Важные предупреждения

### ⚠️ Безопасность ключей

1. **Service Role Key** - **НИКОГДА** не используйте на frontend!
2. **Database URL** содержит пароль - храните в секрете!
3. **Не коммитьте** `.env` файл в Git!
4. Убедитесь, что `.env` в `.gitignore`

### ✅ Проверка .gitignore

Убедитесь, что в `backend/keystonejs-backend/.gitignore` есть:

```
.env
.env.local
.env.*.local
```

---

## 📸 Визуальное описание (текстовое)

### Settings > API страница выглядит так:

```
┌─────────────────────────────────────────┐
│ Settings > API                          │
├─────────────────────────────────────────┤
│                                         │
│ Project URL                             │
│ ┌─────────────────────────────────────┐ │
│ │ https://xxx.supabase.co             │ │ ← SUPABASE_URL
│ └─────────────────────────────────────┘ │
│                                         │
│ Project API keys                        │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ anon         public                  │ │
│ │ eyJhbGc...                           │ │ ← SUPABASE_ANON_KEY
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ service_role secret                 │ │
│ │ eyJhbGc...                           │ │ ← SUPABASE_SERVICE_ROLE_KEY
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### Settings > Database страница выглядит так:

```
┌─────────────────────────────────────────┐
│ Settings > Database                     │
├─────────────────────────────────────────┤
│                                         │
│ Connection string                       │
│ ┌─────────────────────────────────────┐ │
│ │ URI                                  │ │
│ │ postgresql://postgres:[PASSWORD]@... │ │ ← SUPABASE_DATABASE_URL
│ └─────────────────────────────────────┘ │
│                                         │
│ Database password                       │
│ [Reset database password]                │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🆘 Если что-то не получается

### Проблема: Не вижу ключи

**Решение:**
- Убедитесь, что вы вошли в правильный проект
- Проверьте, что у вас есть права администратора проекта

### Проблема: Не знаю пароль базы данных

**Решение:**
1. Settings > Database
2. Прокрутите вниз до "Database password"
3. Нажмите "Reset database password"
4. Сохраните новый пароль
5. Обновите `SUPABASE_DATABASE_URL` в `.env`

### Проблема: Ключи не работают

**Решение:**
- Проверьте, что скопировали ключи полностью (они очень длинные)
- Убедитесь, что нет лишних пробелов
- Проверьте, что используете правильный проект

---

Готово! Теперь у вас должны быть все ключи для настройки проекта. 🎉


