# Исправление ошибки Admin UI

Ошибка `getAdminMetaInRelationshipField` обычно возникает из-за проблем с relationship полями при создании Admin Meta.

## 🔧 Решения

### Решение 1: Очистка кэша (уже сделано)

```bash
rm -rf .keystone node_modules/.cache
npm run dev
```

### Решение 2: Проверка порядка схем

Убедитесь, что в `schemas/index.ts` порядок правильный - базовые схемы (User, Article) должны быть перед зависимыми.

### Решение 3: Self-referencing relationship

В `Comment.ts` есть self-referencing relationship (`parent: relationship({ ref: 'Comment' })`). Я добавил явную конфигурацию UI для него.

### Решение 4: Если ошибка сохраняется

Попробуйте обновить KeystoneJS:

```bash
npm update @keystone-6/core @keystone-6/auth
```

Или переустановите зависимости:

```bash
rm -rf node_modules package-lock.json
npm install
```

### Решение 5: Временный workaround

Если ничего не помогает, можно временно закомментировать проблемное relationship поле, запустить backend, и затем раскомментировать.

## 📝 Проверка

После исправлений запустите:

```bash
npm run dev
```

Если ошибка исчезла - отлично! Если нет - нужно будет найти конкретное проблемное поле.


