# Запуск backend после миграции на Supabase

## ✅ Статус миграции

- [x] Миграции Supabase выполнены
- [x] Переменные окружения настроены
- [x] Зависимости установлены
- [x] Backend подключается к Supabase PostgreSQL

## ⚠️ Известная проблема

Ошибка `getAdminMetaInRelationshipField` возникает при создании Admin UI. Это не критично - GraphQL API работает независимо.

## 🚀 Запуск backend

### Вариант 1: С отключенным Admin UI (рекомендуется)

```bash
cd backend/keystonejs-backend
DISABLE_ADMIN_UI=true npm run dev
```

### Вариант 2: Обычный запуск

```bash
cd backend/keystonejs-backend
npm run dev
```

Если увидите ошибку `getAdminMetaInRelationshipField` - это нормально, API все равно работает.

## ✅ Проверка работы

### 1. Проверьте GraphQL API

Откройте в браузере:
```
http://localhost:1337/api/graphql
```

Должен открыться GraphQL Playground.

Попробуйте выполнить:

```graphql
query {
  __typename
}
```

Должен вернуться: `{"data": {"__typename": "Query"}}`

### 2. Проверьте подключение к Supabase

В логах должны быть:
- ✅ PostgreSQL configuration detected
- ✅ Supabase client configured successfully (если настроен)

### 3. Проверьте таблицы

В Supabase Dashboard > Table Editor должны быть все таблицы.

## 📝 Что дальше?

После успешного запуска:

1. **Frontend может работать** - GraphQL API доступен
2. **Admin UI можно настроить позже** - не критично для работы
3. **Можно тестировать API** - все запросы должны работать

## 🆘 Если проблемы

### Backend не запускается

- Проверьте `.env` файл
- Убедитесь, что `SUPABASE_DATABASE_URL` правильный
- Проверьте логи в `logs/error-2025-12-03.log`

### GraphQL API не отвечает

- Проверьте, что backend запущен
- Проверьте порт: `http://localhost:1337/api/graphql`
- Проверьте CORS настройки

### Ошибка подключения к БД

- Проверьте `SUPABASE_DATABASE_URL` в `.env`
- Убедитесь, что пароль правильный
- Проверьте, что IP разрешен в Supabase


