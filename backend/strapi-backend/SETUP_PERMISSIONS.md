# 🔐 Настройка Permissions для публичного доступа к статьям

## Проблема

React приложение получает `403 Forbidden` или `401 Unauthorized` при попытке загрузить статьи.

## Решение

Нужно настроить permissions вручную через Strapi Admin Panel.

### Шаг 1: Откройте админ-панель

```
http://localhost:1337/admin
```

### Шаг 2: Войдите

- **Email**: `admin@example.com`
- **Password**: `Admin123!`

### Шаг 3: Перейдите в настройки permissions

1. В левом меню нажмите **Settings** (⚙️ иконка внизу)
2. В разделе **USERS & PERMISSIONS PLUGIN** нажмите **Roles**
3. Нажмите на роль **Public**

### Шаг 4: Включите permissions для Article

В разделе **Permissions** найдите **Article** и включите следующие галочки:

- ✅ **find** - для GET /api/articles
- ✅ **findOne** - для GET /api/articles/:id

### Шаг 5: Сохраните

Нажмите кнопку **Save** в правом верхнем углу.

### Шаг 6: Перезагрузите React приложение

Откройте `http://localhost:5174` и перезагрузите страницу (Cmd+R или Ctrl+R).

## Проверка

После настройки permissions:

1. Откройте консоль браузера (F12)
2. Перезагрузите страницу
3. Проверьте Network tab - запросы к `/api/articles` должны возвращать `200 OK`

## Если все еще не работает

1. Проверьте что Strapi запущен: `http://localhost:1337`
2. Проверьте что React запущен: `http://localhost:5174`
3. Очистите cookies и перезагрузите страницу
4. Проверьте консоль браузера на наличие CORS ошибок

## Создание тестовой статьи

Чтобы увидеть статьи на главной странице:

1. В админ-панели перейдите в **Content Manager**
2. Выберите **Article** в левом меню
3. Нажмите **Create new entry**
4. Заполните:
   - **Title**: "Моя первая статья"
   - **Content**: "Это тестовая статья"
   - **Tags**: ["test", "demo"]
5. Нажмите **Publish** (не Save as draft!)
6. Перезагрузите React приложение

## Автоматическая настройка (альтернатива)

Можно попробовать запустить скрипт:

```bash
cd backend/strapi-backend
node fix-permissions.js
```

Затем перезапустить Strapi:

```bash
pkill -f "strapi develop"
npm run dev
```

---

**После настройки permissions статьи должны загружаться!** 🎉

