# 🔐 Настройка прав доступа для статей

## КРИТИЧЕСКИ ВАЖНО!

В Strapi v5 даже если в маршруте указано `auth: false`, права доступа все равно проверяются через систему ролей.

## Настройка прав доступа

### 1. Откройте Strapi Admin

Перейдите: http://localhost:1337/admin

### 2. Настройте роль "Public" (для неавторизованных пользователей)

1. Перейдите: **Settings** (⚙️ слева внизу) → **USERS & PERMISSIONS PLUGIN** → **Roles**
2. Кликните на **Public**
3. Прокрутите вниз до раздела **Permissions**
4. Найдите **Article** (или `api::article.article`) в списке
5. Поставьте галочки:
   - ✅ **find** - для получения списка опубликованных статей (`GET /api/articles`)
   - ✅ **findOne** - для получения одной статьи (`GET /api/articles/:id`)
6. **Нажмите "Save"** (справа вверху)

### 3. Настройте роль "Authenticated" (для авторизованных пользователей)

1. Кликните на **Authenticated**
2. Прокрутите вниз до раздела **Permissions**
3. Найдите **Article** (или `api::article.article`) в списке
4. Поставьте галочки:
   - ✅ **find** - для получения списка статей
   - ✅ **findOne** - для получения одной статьи
   - ✅ **create** - для создания статей (`POST /api/articles`)
   - ✅ **update** - для обновления своих статей (`PUT /api/articles/:id`)
   - ✅ **delete** - для удаления своих статей (`DELETE /api/articles/:id`)
5. **Нажмите "Save"** (справа вверху)

## Проверка

После настройки прав:
1. Обновите страницу в браузере
2. Проверьте, что статьи загружаются на HomePage
3. Проверьте логи в терминале Strapi - должны появиться сообщения о публикации статей

## Важно

- Без прав на `find` и `findOne` для роли `Public` статьи не будут возвращаться через API, даже если они опубликованы
- Контроллер дополнительно проверяет авторство при обновлении/удалении, даже если права настроены
- В Strapi v5 права доступа работают на уровне `entityService`, поэтому они обязательны

