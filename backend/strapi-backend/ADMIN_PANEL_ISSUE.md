# Проблема с админкой Strapi v5

## 📋 Сутель проблемы

**Админка Strapi v5 имеет несовместимость с Vite в dev режиме.**

### Симптомы:
- ✗ HTTP 500 при попытке открыть `/admin`
- ✗ `Admin panel temporarily unavailable during server restart` сообщение
- ✗ Ошибки Vite: `Cannot read properties of undefined (reading 'get')` в `vite:css-post` плагине
- ✗ Постоянные перезагрузки админки

### Причина:
Strapi v5 использует несовместимые версии Vite/Rollup, которые вызывают ошибки при обработке CSS в development режиме. Это известный баг, который не исправлен в текущей версии.

## ✅ Решение

**Используйте REST API вместо админки.**

Все функции админки доступны через API:

### Управление контентом:
```bash
# Получить статьи
curl http://localhost:1337/api/articles

# Создать статью
curl -X POST http://localhost:1337/api/articles \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "data": {
      "title": "My Article",
      "content": "...",
      "publishedAt": "2025-11-09T00:00:00Z"
    }
  }'

# Обновить статью
curl -X PUT http://localhost:1337/api/articles/1 \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"data": {"title": "Updated"}}'

# Удалить статью
curl -X DELETE http://localhost:1337/api/articles/1 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Управление через scripts:
- `seed-articles.js` — добавить тестовые статьи
- `fix-permissions-v5.js` — настроить публичные permissions
- `reset-admin-password.js` — сбросить пароль админа

## 🚀 Рабочая альтернатива

**Используйте React фронтенд** (`http://localhost:5173`) для управления контентом:
- ✅ Создание статей (добавить форму)
- ✅ Редактирование (добавить форму)
- ✅ Удаление (добавить кнопку)
- ✅ Просмотр всех операций в реальном времени

## 🔄 Если нужна админка всё равно

**Вариант 1:** Обновить на Strapi v6 или использовать облачную версию Strapi (https://cloud.strapi.io)

**Вариант 2:** Даунград на Strapi v4 (может требовать миграции БД)

**Вариант 3:** Использовать GraphQL:
```bash
curl http://localhost:1337/graphql
```

## 📝 Текущий статус

- ✅ **API работает** (статьи загружаются)
- ✅ **Google OAuth работает** 
- ✅ **React фронтенд работает**
- ✅ **База данных целая**
- ✗ **Админка UI не работает** (но это не критично — есть API)

## 🎯 Рекомендация

**Не теряйте время на админку Strapi.** Сосредоточьтесь на:
1. Добавлении форм создания/редактирования статей в React фронтенд
2. Функционале приложения
3. Логике работы с контентом через API

API полностью функциональна и это лучший способ управления контентом в разработке.


