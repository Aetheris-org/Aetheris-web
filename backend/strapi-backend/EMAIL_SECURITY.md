# Email Security: HMAC-SHA256 Hashing

## Обзор

Для защиты персональных данных пользователей и соответствия требованиям GDPR/CCPA, наше приложение **не хранит реальные email адреса** пользователей, полученные через Google OAuth2.

Вместо этого используется **HMAC-SHA256 хеширование** с секретным ключом для генерации псевдо-email адресов вида:

```
hash-3d0468067b2f65e3@internal.local
```

## Преимущества

### 🔒 Безопасность
- **Необратимость**: Реальный email невозможно восстановить из хеша без знания секрета
- **Защита от утечек**: В случае утечки БД злоумышленники не получат реальные email пользователей
- **Соответствие GDPR/CCPA**: Минимизация хранения персональных данных

### ✅ Функциональность
- **Детерминированность**: Один и тот же email всегда дает один и тот же хеш
- **Идентификация**: Пользователь успешно находится при повторной авторизации
- **Уникальность**: Каждый email дает уникальный хеш

### 🎯 Практичность
- **Прозрачность**: OAuth2 авторизация работает без изменений для пользователей
- **Совместимость**: Strapi принимает псевдо-email как валидный email формат
- **Миграция**: Существующие пользователи легко мигрируются скриптом

## Техническая реализация

### Алгоритм хеширования

```typescript
import crypto from 'crypto';

function generatePseudoEmail(realEmail: string): string {
  const secret = process.env.EMAIL_HASH_SECRET;
  
  // Нормализация: lowercase + trim
  const normalizedEmail = realEmail.toLowerCase().trim();
  
  // HMAC-SHA256
  const hmac = crypto
    .createHmac('sha256', secret)
    .update(normalizedEmail)
    .digest('hex');
  
  // Первые 16 символов хеша + @internal.local
  return `hash-${hmac.substring(0, 16)}@internal.local`;
}
```

### Конфигурация

**Файл: `backend/strapi-backend/.env`**

```env
EMAIL_HASH_SECRET=6f55de8ada8563c27fdb0caae3c620e00f1215ad10125d3b40dc854d95c4932d
```

⚠️ **КРИТИЧЕСКИ ВАЖНО:**
- Секрет должен быть минимум 64 символа (32 байта в hex)
- Секрет должен быть случайным и уникальным
- **НЕ МЕНЯЙТЕ** секрет после начала использования (иначе существующие пользователи не смогут войти)
- Храните секрет в безопасности (`.env` не должен попадать в Git)

### Генерация нового секрета

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## Интеграция в OAuth2

**Файл: `backend/strapi-backend/src/extensions/users-permissions/strapi-server.ts`**

OAuth callback автоматически:
1. Получает реальный email от Google
2. Генерирует псевдо-email через `generatePseudoEmail()`
3. Ищет пользователя по псевдо-email
4. Создает нового пользователя с псевдо-email (если не найден)
5. Возвращает JWT токен

```typescript
const googleUser = await fetchGoogleUserInfo(accessToken);
const pseudoEmail = generatePseudoEmail(googleUser.email);

let user = await strapi.db.query('plugin::users-permissions.user').findOne({
  where: { email: pseudoEmail },
});

if (!user) {
  user = await strapi.db.query('plugin::users-permissions.user').create({
    data: {
      username: pseudoEmail,
      email: pseudoEmail,
      provider: 'google',
      confirmed: true,
      blocked: false,
      role: defaultRole.id,
    },
  });
}
```

## Миграция существующих пользователей

### Автоматическая миграция

**Скрипт: `backend/strapi-backend/scripts/migrate-emails-to-hash.js`**

```bash
# 1. Остановите Strapi сервер
pkill -f "strapi develop"

# 2. Убедитесь что EMAIL_HASH_SECRET установлен в .env
grep EMAIL_HASH_SECRET backend/strapi-backend/.env

# 3. Запустите скрипт миграции
cd backend/strapi-backend
node scripts/migrate-emails-to-hash.js

# 4. Проверьте результат
# 5. Запустите Strapi обратно
npm run dev
```

### Что делает скрипт

1. Читает всех пользователей с `provider = 'google'`
2. Для каждого пользователя:
   - Проверяет, не является ли email уже псевдо-email
   - Генерирует новый псевдо-email из реального email
   - Обновляет поле `email`
   - Обновляет поле `username` (если оно равно email)
3. Сохраняет лог миграции в JSON файл

### Пример вывода

```
🔵 Starting email migration to pseudo-emails...

✅ EMAIL_HASH_SECRET found (length: 64 chars)

📊 Found 1 Google users

✅ User ID 1: Migrated
   Old email: user@gmail.com
   New email: hash-3d0468067b2f65e3@internal.local

═══════════════════════════════════════
📊 Migration Summary
═══════════════════════════════════════
Total users found: 1
Migrated: 1
Skipped (already hashed): 0

Migration log saved to: scripts/migration-log-1762252592517.json
═══════════════════════════════════════

✅ Migration completed successfully!
```

## Логирование

Все логи в `strapi-server.ts` **НЕ выводят реальные email**:

```typescript
// ❌ БЫЛО (небезопасно):
console.log('User email:', googleUser.email);

// ✅ СТАЛО (безопасно):
console.log('User created with hashed email (ID:', user.id, ')');
```

## Дополнительная защита от утечек

### Временный username при создании

При создании нового пользователя устанавливается временный username вида `user_1730745600000`, а **НЕ** псевдо-email.

**Почему это важно:**
- ❌ Раньше: `username = hash-3d0468067b2f65e3@internal.local`
- ✅ Теперь: `username = user_1730745600000`

**Проблема с показом хеша:**
```typescript
// ПЛОХО (утечка части хеша):
const emailUsername = auth.user.email.split('@')[0]
// Результат: "hash-3d0468067b2f65e3" показывается пользователю

// ХОРОШО (не показываем ничего):
nickname.value = ''
// Пользователь сам вводит никнейм
```

### Валидация username

Router и компоненты проверяют что username **не является:**
- Псевдо-email (`hash-xxxxx@internal.local`)
- Частью хеша (`hash-xxxxx`)
- Временным username (`user_xxxxx`)
- Реальным email

```typescript
const hasValidUsername = userData.username && 
                         userData.username !== userData.email &&
                         !userData.username.startsWith('user_') &&
                         !userData.username.startsWith('hash-')

if (!hasValidUsername) {
  // Перенаправляем на /auth/finalize для ввода настоящего никнейма
}
```

## Ограничения и компромиссы

### ❌ Невозможно:
- Отправлять email пользователям (но для нашего кейса это плюс)
- Восстановить реальный email из БД
- Связаться с пользователем по email
- Видеть хеш email во фронтенде (защита от утечек)

### ✅ Возможно:
- OAuth2 авторизация работает нормально
- Пользователи входят и выходят без проблем
- Уникальная идентификация каждого пользователя
- В админке Strapi видны псевдо-email (для управления)
- Безопасный ввод никнейма без раскрытия хеша

## Безопасность секрета

### Где НЕ хранить:
- ❌ В Git репозитории
- ❌ В фронтенд коде
- ❌ В открытых конфиг файлах
- ❌ В логах

### Где хранить:
- ✅ В `.env` файле (добавлен в `.gitignore`)
- ✅ В переменных окружения сервера
- ✅ В секретных менеджерах (AWS Secrets Manager, Vault и т.д.)

### Резервное копирование:
- Сохраните `EMAIL_HASH_SECRET` в надежном месте
- При потере секрета ВСЕ существующие пользователи не смогут войти
- Можно будет только удалить всех пользователей и начать заново

## Проверка работоспособности

### 1. Новый пользователь
1. Перейдите на `http://localhost:5173/auth`
2. Нажмите "Продолжить с Google"
3. Авторизуйтесь через Google (с новым email)
4. Проверьте в Strapi Admin → Users:
   - Email должен быть вида `hash-xxxxxx@internal.local`
5. Вы должны попасть на `/auth/finalize` для ввода никнейма

### 2. Существующий пользователь
1. Выйдите из аккаунта
2. Снова войдите через Google (с тем же email)
3. Система должна найти существующего пользователя по псевдо-email
4. Вы должны попасть на главную страницу (не на `/auth/finalize`)

### 3. Проверка БД
```bash
cd backend/strapi-backend
sqlite3 .tmp/data.db "SELECT id, email, username FROM up_users WHERE provider = 'google';"
```

Вывод должен показать только псевдо-email:
```
1|hash-3d0468067b2f65e3@internal.local|ZimBazo|google
```

## FAQ

### Q: Что если я потеряю EMAIL_HASH_SECRET?
**A:** Все существующие пользователи не смогут войти, так как новый секрет будет генерировать другие хеши. Придется удалить всех пользователей и начать заново.

### Q: Можно ли изменить алгоритм хеширования?
**A:** Да, но нужно будет перемигрировать всех пользователей заново. Убедитесь что новый алгоритм тоже детерминированный.

### Q: Что если пользователь хочет удалить свои данные (GDPR Right to Erasure)?
**A:** Просто удалите пользователя из Strapi Admin. Реальный email нигде не хранится, так что удаление пользователя удаляет все связанные данные.

### Q: Как отличить одного пользователя от другого в админке?
**A:** По `username` (который пользователь вводит на `/auth/finalize`) и по `id`. Псевдо-email тоже уникален, но не читабелен.

### Q: Может ли злоумышленник подобрать email по хешу?
**A:** Нет, если секрет достаточно длинный (64+ символа) и случайный. HMAC-SHA256 криптографически стойкий к подбору.

### Q: Что если два пользователя введут одинаковый email?
**A:** Невозможно — Google OAuth2 возвращает только подтвержденные уникальные email. Даже если бы это было возможно, они получат одинаковый псевдо-email и будут считаться одним пользователем.

## Заключение

Эта реализация обеспечивает:
- ✅ Полную защиту персональных данных
- ✅ Соответствие GDPR/CCPA
- ✅ Минимизацию рисков при утечке данных
- ✅ Прозрачную работу OAuth2
- ✅ Простую миграцию существующих пользователей

При этом используются только стандартные инструменты Node.js (`crypto`) без внешних зависимостей.

