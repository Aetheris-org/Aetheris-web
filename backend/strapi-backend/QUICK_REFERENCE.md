# Email Security - Quick Reference

## ✅ Что сделано

Реальные email пользователей теперь **не хранятся** в базе данных. Вместо них используются безопасные HMAC-хеши.

## 🔍 Как проверить

### 1. Проверить БД
```bash
cd backend/strapi-backend
sqlite3 .tmp/data.db "SELECT id, email, username FROM up_users WHERE provider = 'google';"
```

**Ожидаемый результат:**
```
1|hash-3d0468067b2f65e3@internal.local|ZimBazo|google
```

### 2. Войти как существующий пользователь
1. Откройте http://localhost:5173/auth
2. Нажмите "Продолжить с Google"
3. Войдите с email: **fluy1337@gmail.com**
4. Должны попасть на главную (не на /auth/finalize)

### 3. Войти как новый пользователь
1. Откройте http://localhost:5173/auth
2. Нажмите "Продолжить с Google"
3. Войдите с **другим** Google аккаунтом
4. Должны попасть на /auth/finalize для ввода никнейма

### 4. Проверить Strapi Admin
1. Откройте http://localhost:1337/admin
2. Content Manager → Users
3. Email должен быть: `hash-xxxxxx@internal.local`

## 🔐 Секретный ключ

**Файл:** `backend/strapi-backend/.env`

```env
EMAIL_HASH_SECRET=6f55de8ada8563c27fdb0caae3c620e00f1215ad10125d3b40dc854d95c4932d
```

⚠️ **ВАЖНО:**
- **НЕ МЕНЯЙТЕ** этот секрет после начала использования
- **НЕ КОММИТЬТЕ** .env в Git
- **СОХРАНИТЕ** секрет в безопасном месте

## 📝 Полезные команды

### Перезапустить Strapi
```bash
cd backend/strapi-backend
pkill -f "strapi develop"  # Остановить
npm run dev                # Запустить
```

### Проверить хеш для email
```bash
cd backend/strapi-backend
node -e "
const crypto = require('crypto');
require('dotenv').config();
const email = 'test@gmail.com';
const secret = process.env.EMAIL_HASH_SECRET;
const hmac = crypto.createHmac('sha256', secret).update(email.toLowerCase()).digest('hex');
console.log('Email:', email);
console.log('Hash:', 'hash-' + hmac.substring(0, 16) + '@internal.local');
"
```

### Повторная миграция (если нужно)
```bash
cd backend/strapi-backend
pkill -f "strapi develop"           # Остановить Strapi
node scripts/migrate-emails-to-hash.js  # Мигрировать
npm run dev                         # Запустить
```

## 📚 Документация

- **Полная документация:** `backend/strapi-backend/EMAIL_SECURITY.md`
- **Сводка изменений:** `IMPLEMENTATION_SUMMARY.md`
- **План:** `google.plan.md`

## 🆘 Что делать если...

### Пользователь не может войти
1. Проверьте что Strapi запущен: `lsof -i:1337`
2. Проверьте логи: `cd backend/strapi-backend && npm run dev`
3. Проверьте БД: `sqlite3 .tmp/data.db "SELECT * FROM up_users;"`

### Нужно сменить секрет (ОПАСНО!)
1. Остановите Strapi
2. Запишите старый `EMAIL_HASH_SECRET`
3. Измените на новый
4. **УДАЛИТЕ ВСЕХ ПОЛЬЗОВАТЕЛЕЙ:** `sqlite3 .tmp/data.db "DELETE FROM up_users WHERE provider = 'google';"`
5. Запустите Strapi
6. Все пользователи должны войти заново

### Email утек из логов
- ✅ В новом коде логи не содержат реальных email
- ✅ Проверьте что используете последнюю версию `strapi-server.ts`
- ✅ Перезапустите Strapi чтобы применить изменения

## ✅ Чеклист готовности

- [x] EMAIL_HASH_SECRET добавлен в .env
- [x] Функция generatePseudoEmail() реализована
- [x] OAuth callback использует псевдо-email
- [x] Логи не выводят реальные email
- [x] Существующие пользователи мигрированы
- [x] Strapi запущен и работает
- [x] Документация создана

## 🎉 Готово!

Теперь ваше приложение защищено от утечек email и соответствует GDPR/CCPA.

---

**Вопросы?** Читайте `EMAIL_SECURITY.md` (FAQ внизу)

