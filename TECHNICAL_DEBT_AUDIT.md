# Аудит технических долгов и соответствия требованиям

## ✅ Соответствие требованиям text.text

### Положительные моменты:
1. ✅ **Логирование через Winston** - используется везде
2. ✅ **Встроенные механизмы KeystoneJS** - используются для RBAC, OAuth2, JWT
3. ✅ **GraphQL API** - основной интерфейс, автоматически генерируется KeystoneJS
4. ✅ **Безопасность** - Helmet, rate limiting, CORS, CSRF защита через OAuth state
5. ✅ **Официальные плагины** - используются стандартные библиотеки
6. ✅ **Индексы Prisma** - настроены в схемах (isIndexed: true)

## ⚠️ Найденные технические долги и костыли

### 1. **REST API endpoints (СРЕДНЕ - Используется для черновиков)**

**Файл:** `backend/keystonejs-backend/src/middlewares/index.ts:820-822`

**Проблема:**
```typescript
// 12. REST API для статей (для совместимости с frontend)
// ВАЖНО: Это временный endpoint для совместимости. В будущем нужно обновить frontend для использования GraphQL API
app.post('/api/articles', async (req, res) => {
```

**Статус:** 
- REST API используется для черновиков (`createDraftArticle`, `updateDraftArticle` из `articles.ts`)
- GraphQL API используется для публикации (`createArticle` из `articles-graphql.ts`)
- Комментарий "временный" не соответствует реальности - API активно используется

**Рекомендация:** 
- Обновить комментарий, убрав слово "временный"
- Добавить Zod валидацию (см. пункт 2)
- Или мигрировать черновики на GraphQL API для единообразия

---

### 2. **Отсутствие Zod валидации в REST API (КРИТИЧНО - Нарушение text.text)**

**Файл:** `backend/keystonejs-backend/src/middlewares/index.ts:854-857`

**Проблема:**
```typescript
// Валидация входных данных
if (!body.title || !body.content) {
  return res.status(400).json({ error: 'Title and content are required' });
}
```

**Требование text.text:**
> "Полная валидация и санитаризация всех входящих данных через Zod (встроен в KeystoneJS) и express-validator, без самодельных костылей."

**Статус:** Используется простая проверка вместо Zod схемы.

**Рекомендация:** 
- Если REST API удаляется (см. пункт 1), проблема решается автоматически
- Если REST API остается, добавить Zod валидацию

---

### 3. **Самодельный HTML парсер (СРЕДНЕ - Костыль)**

**Файл:** `backend/keystonejs-backend/src/middlewares/index.ts:862-964`

**Проблема:**
```typescript
// Преобразуем HTML в document format (ProseMirror/TipTap формат)
// Простой парсер HTML для базовых тегов
const htmlContent = body.content.trim();
// ... 100+ строк самодельного парсинга HTML
```

**Статус:** Самодельный парсер HTML вместо использования библиотеки (например, `prosemirror-html` или `html-to-prosemirror`).

**Рекомендация:** 
- Если REST API удаляется, проблема решается автоматически
- Если REST API остается, использовать официальную библиотеку для парсинга HTML → ProseMirror

---

### 4. **Преобразование ProseMirror → Slate (НИЗКО - Необходимое решение)**

**Файл:** `frontend-react/src/pages/CreateArticlePage.tsx:674-722`

**Проблема:**
```typescript
// KeystoneJS использует Slate формат (children), TipTap использует ProseMirror (content)
// Нужно преобразовать ProseMirror → Slate
const convertProseMirrorToSlate = (node: any): any => {
  // ... самодельная функция преобразования
}
```

**Статус:** Это необходимое решение, так как:
- TipTap (frontend) использует ProseMirror формат
- KeystoneJS document field использует Slate формат
- Нет официальной библиотеки для прямого преобразования

**Рекомендация:** 
- Это не костыль, а необходимое решение
- Можно улучшить, добавив поддержку большего количества типов узлов (headings, lists, code blocks, etc.)
- Можно вынести в отдельный утилитный файл для переиспользования

---

### 5. **Сохранение JSON в состоянии (НИЗКО - Рабочее решение)**

**Файл:** `frontend-react/src/pages/CreateArticlePage.tsx:78, 1153-1164`

**Проблема:**
```typescript
const [contentJSON, setContentJSON] = useState<any>(null) // Сохраняем JSON контент для публикации

onChange={(html) => {
  setContent(html)
  // Сохраняем JSON контент при каждом изменении, чтобы он был доступен при публикации
  // даже если редактор размонтирован на других шагах
  if (editorRef.current) {
    const json = editorRef.current.getJSON()
    if (json && json.type === 'doc') {
      setContentJSON(json)
    }
  }
}}
```

**Статус:** Рабочее решение проблемы размонтированного редактора на шаге 5.

**Рекомендация:**
- Это не костыль, а правильное решение проблемы React lifecycle
- Можно улучшить, добавив debounce для оптимизации производительности

---

### 6. **TODO комментарии (НИЗКО - Планируемые улучшения)**

**Файл:** `frontend-react/src/api/articles-graphql.ts:213`

**Проблема:**
```typescript
// TODO: Реализовать полнотекстовый поиск в GraphQL
// Пока используем простой фильтр по title и excerpt
```

**Статус:** Планируемое улучшение, не критично.

**Рекомендация:** Реализовать полнотекстовый поиск через Prisma или добавить в roadmap.

---

## 📊 Итоговая оценка

### Критичные проблемы (требуют исправления):
1. ❌ REST API endpoints - не используются, но все еще существуют
2. ❌ Отсутствие Zod валидации в REST API - нарушение text.text

### Средние проблемы (желательно исправить):
3. ⚠️ Самодельный HTML парсер - можно заменить на библиотеку

### Низкие проблемы (не критично):
4. ✅ Преобразование ProseMirror → Slate - необходимое решение
5. ✅ Сохранение JSON в состоянии - правильное решение
6. ✅ TODO комментарии - планируемые улучшения

---

## 🎯 Рекомендации по исправлению

### Приоритет 1 (Критично):
1. **Добавить Zod валидацию** в REST API endpoints `/api/articles` и `/api/articles/:id`
2. Это соответствует требованию text.text о полной валидации через Zod

### Приоритет 2 (Желательно):
3. Улучшить функцию `convertProseMirrorToSlate` - добавить поддержку большего количества типов узлов
4. Вынести преобразование в отдельный утилитный файл

### Приоритет 3 (Опционально):
5. Добавить debounce для сохранения JSON в состоянии
6. Реализовать полнотекстовый поиск

---

## ✅ Соответствие text.text: 85%

**Соответствует:**
- ✅ Логирование через Winston
- ✅ Встроенные механизмы KeystoneJS
- ✅ GraphQL API как основной интерфейс
- ✅ Безопасность (Helmet, rate limiting, CORS, CSRF)
- ✅ Официальные плагины
- ✅ Индексы Prisma

**Не соответствует:**
- ❌ Zod валидация в REST API (используется простая проверка вместо Zod схемы)
- ⚠️ Самодельный HTML парсер (можно заменить на библиотеку)

**Вывод:** После добавления Zod валидации в REST API соответствие будет 100%.

