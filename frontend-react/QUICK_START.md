# 🚀 Quick Start - Aetheris React

## Быстрый запуск за 3 шага

### 1️⃣ Установка

```bash
cd frontend-react
npm install
```

### 2️⃣ Настройка

Создайте файл `.env`:

```bash
cp .env.example .env
```

Убедитесь что содержимое `.env`:
```env
VITE_API_BASE_URL=http://localhost:1337
```

### 3️⃣ Запуск

```bash
npm run dev
```

Откройте браузер: **http://localhost:5174**

---

## ✅ Что уже работает

- ✅ Главная страница со списком статей
- ✅ Поиск по статьям
- ✅ Фильтрация (сложность, время чтения, теги, категории)
- ✅ Пагинация
- ✅ Trending статьи в сайдбаре
- ✅ 3 режима отображения карточек (default, line, square)
- ✅ Удаление статей (с подтверждением)
- ✅ Toast уведомления
- ✅ Темная тема
- ✅ Адаптивный дизайн
- ✅ Cookie-based аутентификация
- ✅ CSRF защита
- ✅ Auto-refresh токенов

## 🎨 Компоненты

### UI компоненты (shadcn/ui)
- Button
- Card
- Dialog
- Input
- Badge
- Toast
- Toaster

### Кастомные компоненты
- **ArticleCard** - полная карточка статьи с превью
- **ArticleCardLine** - компактная линия
- **ArticleCardSquare** - квадратная карточка для grid

## 📁 Структура

```
src/
├── api/              # API клиенты (articles.ts)
├── components/       # React компоненты
│   ├── ui/          # shadcn/ui компоненты
│   ├── ArticleCard.tsx
│   ├── ArticleCardLine.tsx
│   └── ArticleCardSquare.tsx
├── lib/             # Утилиты (axios, strapi, utils)
├── pages/           # Страницы (HomePage.tsx)
├── stores/          # Zustand stores (auth, viewMode)
├── types/           # TypeScript типы
└── App.tsx          # Главный компонент
```

## 🔧 Доступные команды

```bash
npm run dev      # Dev сервер (порт 5174)
npm run build    # Production сборка
npm run preview  # Предпросмотр production
npm run lint     # Проверка кода
```

## 🎯 Следующие шаги

### Что можно добавить:

1. **Страница статьи** (`/article/:id`)
   - Полный текст статьи
   - Комментарии
   - Лайки/дизлайки

2. **Профиль пользователя** (`/profile/:id`)
   - Информация о пользователе
   - Статьи пользователя
   - Статистика

3. **Создание статьи** (`/create`)
   - Редактор markdown
   - Загрузка изображений
   - Выбор тегов

4. **Дополнительные функции**:
   - Report panel (жалобы)
   - Share panel (шаринг)
   - Закладки
   - Подписки

### Добавление shadcn/ui компонентов:

```bash
npx shadcn-ui@latest add dropdown-menu
npx shadcn-ui@latest add select
npx shadcn-ui@latest add avatar
npx shadcn-ui@latest add tabs
```

## 🐛 Troubleshooting

### Проблема: Не запускается

```bash
# Очистить и переустановить
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Проблема: CORS ошибки

Убедитесь что Strapi backend запущен на `http://localhost:1337` и настроен CORS для `http://localhost:5174`.

### Проблема: TypeScript ошибки

```bash
# Перезапустить TypeScript сервер в IDE
# Или проверить tsconfig.json
```

## 📚 Документация

- [Полное руководство](./README.md)
- [Гайд по миграции](../REACT_MIGRATION_GUIDE.md)
- [shadcn/ui](https://ui.shadcn.com/)
- [TanStack Query](https://tanstack.com/query/latest)

## 💡 Советы

- Используйте **React DevTools** для отладки
- Используйте **TanStack Query DevTools** для мониторинга запросов
- Все API запросы кешируются автоматически
- Токены обновляются автоматически при 401

---

**Готово! Теперь у вас есть современный React фронтенд! 🎉**

