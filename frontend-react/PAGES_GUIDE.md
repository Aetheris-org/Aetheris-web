# 📄 Pages Guide - Aetheris React

## Созданные страницы

### ✅ Основные страницы

#### 1. **HomePage** (`/`)
Главная страница со списком статей

**Функционал:**
- 📝 Список статей с пагинацией
- 🔍 Поиск по статьям
- 🏷️ Фильтрация по тегам
- 🔥 Trending статьи в сайдбаре
- 👁️ 3 режима отображения (default, line, square)
- ✍️ Кнопка "Write" для создания статьи (для авторизованных)
- 🔐 Кнопка "Sign In" (для неавторизованных)

**Навигация:**
- Клик по статье → `/article/:id`
- Клик по trending статье → `/article/:id`
- Клик по тегу → фильтрация
- Кнопка "Write" → `/create`
- Кнопка "Sign In" → `/auth`

#### 2. **ArticlePage** (`/article/:id`)
Страница просмотра полной статьи

**Функционал:**
- 📖 Полный текст статьи
- 👤 Информация об авторе
- 📅 Дата публикации
- ⏱️ Время чтения (автоматический расчет)
- 🏷️ Теги
- ❤️ Лайки
- 🔖 Закладки
- 📤 Шаринг
- 💬 Комментарии (coming soon)

**Навигация:**
- Кнопка "Back" → возврат назад
- Клик по тегу → фильтрация на главной

#### 3. **ProfilePage** (`/profile/:id`)
Страница профиля пользователя

**Функционал:**
- 👤 Аватар и информация о пользователе
- 📊 Статистика (статьи, подписчики, подписки)
- 📝 Bio
- 📅 Дата регистрации
- 📑 Вкладки: Articles, Comments
- ⚙️ Кнопка "Edit Profile" (для своего профиля)

**Навигация:**
- Кнопка "Back" → возврат назад
- Кнопка "Edit Profile" → `/settings/profile`

#### 4. **AuthPage** (`/auth`)
Страница авторизации и регистрации

**Функционал:**
- 🔐 Sign In форма
- 📝 Sign Up форма
- 🔄 Переключение между вкладками
- 🌐 OAuth кнопки (Google, GitHub) - coming soon
- 🔗 Forgot password - coming soon

**Навигация:**
- После успешного входа → `/`

#### 5. **CreateArticlePage** (`/create`)
Страница создания статьи

**Функционал:**
- ✍️ Заголовок статьи
- 📝 Excerpt (краткое описание)
- 📄 Контент (textarea с markdown поддержкой - coming soon)
- 🏷️ Добавление тегов
- 🎯 Выбор сложности (easy, medium, hard)
- 💾 Сохранение черновика
- 🚀 Публикация

**Навигация:**
- Кнопка "Back" → возврат назад
- После публикации → `/`

#### 6. **SettingsPage** (`/settings/*`)
Страница настроек с подразделами

**Подразделы:**
- `/settings/profile` - Настройки профиля
- `/settings/appearance` - Внешний вид (тема)
- `/settings/privacy` - Приватность
- `/settings/notifications` - Уведомления
- `/settings/sessions` - Активные сессии
- `/settings/billing` - Биллинг

**Функционал:**
- 📋 Боковое меню навигации
- ⚙️ Различные настройки для каждого раздела
- 💾 Сохранение изменений

**Навигация:**
- Кнопка "Back" → возврат назад
- Клик по разделу → переход между разделами

#### 7. **NotificationsPage** (`/notifications`)
Центр уведомлений с мок-данными (интеграция с backend в разработке)

**Функционал:**
- 🔔 Просмотр уведомлений за сегодня, неделю и более ранние
- 🧭 Быстрые действия: "View" и "Mark as read" (пока без подключения)
- 🧮 Индикатор количества непрочитанных

**Навигация:**
- Кнопка "Back" → возврат назад
- Быстрый доступ через шапку главной страницы и `AccountSheet`
- После подключения Strapi: переходы к конкретным сущностям (статьям, профилям)

---

## 🎨 Дизайн

Все страницы следуют единому дизайну:

### Header
- **Sticky** - прилипает к верху при скролле
- **Backdrop blur** - полупрозрачный фон
- **Кнопка "Back"** - на всех страницах кроме главной
- **ThemeToggle** - переключатель темы
- **Высота**: 64px

### Layout
- **Container**: max-width 1400px, centered
- **Padding**: 32px (py-8)
- **Grid**: для sidebar (lg:grid-cols-[1fr_300px])

### Colors
- **Light theme** по умолчанию
- **Dark theme** через переключатель
- **Минималистичная палитра** - только серые оттенки

### Typography
- **Заголовки**: font-bold, tracking-tight
- **Body**: font-normal, leading-relaxed
- **Muted**: text-muted-foreground

---

## 🔄 Навигация между страницами

```
HomePage (/)
├─→ ArticlePage (/article/:id)
│   └─→ ProfilePage (/profile/:id)
├─→ AuthPage (/auth)
├─→ CreateArticlePage (/create)
└─→ SettingsPage (/settings/*)
    ├─→ /settings/profile
    ├─→ /settings/appearance
    ├─→ /settings/privacy
    ├─→ /settings/notifications
    ├─→ /settings/sessions
    └─→ /settings/billing
```

---

## 📱 Адаптивность

Все страницы адаптивны:

### Breakpoints
- **Mobile**: < 640px
- **Tablet**: 640px - 1024px
- **Desktop**: > 1024px

### Особенности
- **Sidebar** скрывается на мобильных
- **Grid** переключается на single column
- **Buttons** адаптируются по размеру
- **Typography** масштабируется

---

## 🚀 Как добавить новую страницу

1. Создайте файл в `/src/pages/`:
```tsx
// src/pages/NewPage.tsx
import { useNavigate } from 'react-router-dom'
import { ArrowLeft } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { ThemeToggle } from '@/components/ThemeToggle'

export default function NewPage() {
  const navigate = useNavigate()

  return (
    <div className="min-h-screen bg-background">
      <header className="sticky top-0 z-50 w-full border-b bg-background/95 backdrop-blur">
        <div className="container flex h-16 items-center justify-between">
          <Button variant="ghost" size="sm" onClick={() => navigate(-1)}>
            <ArrowLeft className="mr-2 h-4 w-4" />
            Back
          </Button>
          <ThemeToggle />
        </div>
      </header>

      <div className="container py-8">
        {/* Your content here */}
      </div>
    </div>
  )
}
```

2. Добавьте роут в `App.tsx`:
```tsx
import NewPage from '@/pages/NewPage'

// В Routes:
<Route path="/new" element={<NewPage />} />
```

3. Добавьте навигацию:
```tsx
navigate('/new')
```

---

## 🎯 Coming Soon

Страницы которые можно добавить:

- **EditArticlePage** (`/edit/:id`) - редактирование статьи
- **YourArticlesPage** (`/my-articles`) - ваши статьи
- **DraftArticlesPage** (`/drafts`) - черновики
- **BookmarksPage** (`/bookmarks`) - закладки
- **NotificationsPage** (`/notifications`) - уведомления
- **SearchPage** (`/search`) - расширенный поиск
- **TagPage** (`/tag/:name`) - статьи по тегу

---

**Все страницы готовы к использованию! 🎉**

