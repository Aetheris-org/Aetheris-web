# 🚀 Деплой фронтенда на Render с Supabase

## 📋 Как это работает:

**Фронтенд → Supabase (напрямую через REST API)**

- ❌ **НЕТ** отдельного бэкенд-сервера
- ✅ Фронтенд общается с Supabase напрямую через `@supabase/supabase-js`
- ✅ Supabase работает в облаке автоматически

## 🔧 Настройка на Render:

### 1️⃣ Создайте Static Site на Render

1. Зайдите на https://render.com
2. **New** → **Static Site**
3. Подключите ваш GitHub репозиторий
4. Выберите:
   - **Root Directory**: `frontend-react`
   - **Build Command**: `npm run build`
   - **Publish Directory**: `dist`

### 2️⃣ Настройте Environment Variables

В настройках Static Site на Render добавьте:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

**Где взять ключи:**
1. Supabase Dashboard → **Settings** → **API**
2. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public** key → `VITE_SUPABASE_ANON_KEY`

### 3️⃣ Настройте OAuth Redirect URLs в Supabase

1. Supabase Dashboard → **Authentication** → **URL Configuration**
2. Добавьте в **Redirect URLs**:
   ```
   https://your-app.onrender.com/auth/callback
   ```
3. Также добавьте для development (если нужно):
   ```
   http://localhost:5173/auth/callback
   ```

### 4️⃣ Настройте CORS в Supabase (если нужно)

1. Supabase Dashboard → **Settings** → **API**
2. В **CORS** добавьте ваш домен Render:
   ```
   https://your-app.onrender.com
   ```

### 5️⃣ Деплой

1. Нажмите **Deploy** на Render
2. Дождитесь завершения билда
3. Ваш сайт будет доступен по адресу: `https://your-app.onrender.com`

## ✅ Проверка:

После деплоя проверьте:

1. ✅ Сайт открывается
2. ✅ Регистрация/вход работает
3. ✅ Статьи загружаются
4. ✅ OAuth (Google/GitHub) работает
5. ✅ Загрузка изображений работает

## 🔍 Troubleshooting:

### Ошибка: "Missing Supabase environment variables"

**Решение:** Проверьте, что в Render добавлены переменные:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

### OAuth не работает

**Решение:** 
1. Проверьте Redirect URL в Supabase Dashboard
2. Убедитесь, что URL точно совпадает (с `/` в конце или без)

### CORS ошибки

**Решение:**
1. Добавьте ваш домен Render в CORS настройки Supabase
2. Или используйте Supabase CORS по умолчанию (разрешает все)

## 📝 Важно:

- ⚠️ **НЕ добавляйте** `VITE_API_BASE_URL` - старый бэкенд удален
- ✅ Используйте только `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
- ✅ Supabase работает автоматически - ничего запускать не нужно

## 🎯 Итого:

1. Создайте Static Site на Render
2. Добавьте 2 переменные окружения (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
3. Настройте OAuth Redirect URL в Supabase
4. Деплойте!

**Всё! Фронтенд автоматически подключится к Supabase!** 🎉

