# React Migration Guide - Aetheris

## 📋 Что было сделано

Создана полноценная React версия фронтенда Aetheris с использованием современного стека технологий:

### Технологический стек

- ⚛️ **React 18** - современная UI библиотека
- 📘 **TypeScript** - строгая типизация
- ⚡ **Vite** - быстрый сборщик
- 🎨 **shadcn/ui** - красивые UI компоненты на базе Radix UI
- 🎭 **Tailwind CSS** - utility-first CSS фреймворк
- 🔄 **TanStack Query (React Query)** - управление серверным состоянием
- 🐻 **Zustand** - легковесное управление состоянием
- 🛣️ **React Router** - роутинг
- 📡 **Axios** - HTTP клиент с interceptors

### Структура проекта

```
frontend-react/
├── src/
│   ├── api/                    # API клиенты
│   │   └── articles.ts         # Работа со статьями
│   ├── components/
│   │   └── ui/                 # shadcn/ui компоненты
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       ├── dialog.tsx
│   │       ├── input.tsx
│   │       ├── badge.tsx
│   │       ├── toast.tsx
│   │       ├── use-toast.ts
│   │       └── toaster.tsx
│   ├── lib/                    # Утилиты
│   │   ├── axios.ts            # Настроенный axios с auth
│   │   ├── strapi.ts           # Адаптеры для Strapi API
│   │   └── utils.ts            # Хелперы (cn и др.)
│   ├── pages/
│   │   └── HomePage.tsx        # Главная страница со статьями
│   ├── stores/                 # Zustand stores
│   │   ├── authStore.ts        # Состояние аутентификации
│   │   └── viewModeStore.ts    # Режим отображения статей
│   ├── types/                  # TypeScript типы
│   │   ├── article.ts
│   │   └── user.ts
│   ├── App.tsx                 # Главный компонент
│   ├── main.tsx                # Точка входа
│   ├── index.css               # Глобальные стили + Tailwind
│   └── vite-env.d.ts           # TypeScript definitions
├── components.json             # Конфигурация shadcn/ui
├── tailwind.config.js          # Конфигурация Tailwind
├── tsconfig.json               # Конфигурация TypeScript
├── vite.config.ts              # Конфигурация Vite
├── package.json
└── README.md
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd frontend-react
npm install
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Содержимое `.env`:
```env
VITE_API_BASE_URL=http://localhost:1337
```

### 3. Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5174`

### 4. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## 🎯 Реализованные функции

### ✅ Главная страница (HomePage)

- **Поиск статей** - полнотекстовый поиск с debounce
- **Фильтрация**:
  - По сложности (easy/medium/hard)
  - По времени чтения
  - По тегам (множественный выбор)
  - По категориям (all/news/development/research/guides)
- **Отображение статей** - список с карточками
- **Пагинация** - навигация по страницам
- **Trending статьи** - боковая панель с популярными статьями
- **Удаление статей** - с подтверждением через Dialog

### 🎨 UI/UX

- **Темная тема** - по умолчанию
- **Адаптивный дизайн** - работает на всех устройствах
- **Анимации** - плавные переходы и hover эффекты
- **Toast уведомления** - для feedback пользователю
- **Loading states** - спиннеры при загрузке
- **Empty states** - красивые заглушки когда нет данных

### 🔐 Безопасность

- **Cookie-based auth** - токены в HttpOnly cookies
- **CSRF защита** - автоматическая отправка CSRF токенов
- **Auto-refresh токенов** - прозрачное обновление при 401
- **Request cancellation** - отмена запросов при logout

### 📡 API интеграция

- **Strapi v5 адаптеры** - обработка формата данных Strapi
- **Автоматическая типизация** - TypeScript типы для всех API
- **Error handling** - обработка ошибок с retry логикой
- **Query caching** - кеширование с React Query

## 🛠️ Дальнейшая разработка

### Что можно добавить:

1. **Компоненты карточек статей**:
   - `ArticleCard.tsx` - полная карточка (по умолчанию)
   - `ArticleCardLine.tsx` - компактная линия
   - `ArticleCardSquare.tsx` - квадратная карточка

2. **Дополнительные страницы**:
   - Страница статьи (`/article/:id`)
   - Профиль пользователя (`/profile/:id`)
   - Создание/редактирование статьи (`/create`, `/edit/:id`)

3. **Дополнительные модалки**:
   - Report panel - жалобы на статьи
   - Share panel - шаринг в соцсети
   - Edit article dialog

4. **Дополнительные функции**:
   - Лайки/дизлайки статей
   - Комментарии
   - Закладки
   - Подписки на авторов

### Добавление новых shadcn/ui компонентов

```bash
npx shadcn-ui@latest add [component-name]
```

Примеры:
```bash
npx shadcn-ui@latest add dropdown-menu
npx shadcn-ui@latest add select
npx shadcn-ui@latest add avatar
npx shadcn-ui@latest add separator
npx shadcn-ui@latest add tabs
```

## 📚 Полезные ссылки

- [shadcn/ui документация](https://ui.shadcn.com/)
- [Tailwind CSS](https://tailwindcss.com/)
- [TanStack Query](https://tanstack.com/query/latest)
- [Zustand](https://zustand-demo.pmnd.rs/)
- [React Router](https://reactrouter.com/)

## 🔄 Миграция с Vue

### Основные отличия:

| Vue | React |
|-----|-------|
| `ref()`, `reactive()` | `useState()`, `useReducer()` |
| `computed()` | `useMemo()`, `useCallback()` |
| `watch()`, `watchEffect()` | `useEffect()` |
| Pinia stores | Zustand stores |
| Vue Router | React Router |
| `v-if`, `v-for` | `&&`, `.map()` |
| `@click`, `:class` | `onClick`, `className` |

### Преимущества React версии:

- ✅ Более строгая типизация с TypeScript
- ✅ Лучшая экосистема компонентов (shadcn/ui)
- ✅ Более предсказуемый lifecycle
- ✅ Легче тестировать
- ✅ Больше готовых решений и библиотек

## 🐛 Troubleshooting

### Проблема: Ошибки при установке зависимостей

```bash
# Очистить кеш и переустановить
rm -rf node_modules package-lock.json
npm install
```

### Проблема: CORS ошибки

Убедитесь что Strapi backend запущен на `http://localhost:1337` и настроен CORS:

```js
// backend/strapi-backend/config/middlewares.ts
export default [
  // ...
  {
    name: 'strapi::cors',
    config: {
      origin: ['http://localhost:5174'],
      credentials: true,
    },
  },
]
```

### Проблема: Не работает hot reload

```bash
# Перезапустить dev сервер
npm run dev
```

## 📝 Примечания

- Проект использует **темную тему** по умолчанию (можно добавить переключатель)
- Все API запросы проксируются через Vite dev server
- Токены хранятся в **cookies**, а не в localStorage (безопаснее)
- Используется **cookie-based аутентификация** с auto-refresh

## 🎉 Готово!

Теперь у вас есть современный React фронтенд с shadcn/ui, который полностью интегрирован с вашим Strapi backend!

Для запуска:
```bash
cd frontend-react
npm install
npm run dev
```

Приложение откроется на `http://localhost:5174`

